## Progress

- 2026-03-29 help 闭环 47：完成“页面内嵌帮助说明能力”最小闭环，只在前端落统一配置和通用组件，不引入后台帮助中心。`AppPageShell.vue` 新增标题右侧扩展槽位，前端新增 `pageHelp.js`、`PageHelp.vue`、`PageGuide.vue` 三个通用能力：1）标题右侧统一问号入口，可查看当前页面“功能说明 / 主要操作 / 使用提示”；2）首次进入页面自动弹一次轻量提示，并按 `help:first-visit:<pageKey>:<userId|username>` 写入本地缓存，不再重复打扰；3）对配置了 `guideSteps` 的页面提供极简步骤引导，按 `data-guide` 高亮目标区域并在目标缺失时自动跳过，不会白屏。当前已接入 4 个核心页面：组织架构、周报管理、考勤管理、统计分析；分别为组织树/节点详情/模块授权入口、周报分区/编辑区/提交区、考勤查询区/打卡入口/异常榜单、统计查询区/总体概览/趋势区补了稳定 `data-guide` 标记。验证结果：`mvn -q -DskipTests compile` 通过；`nvm use 20 >/dev/null && npm run build` 通过；`DB_HOST=172.24.16.1 mvn -Dspring-boot.run.profiles=wsl spring-boot:run` 启动成功。已保持现有菜单、权限、路由和页面主链不回退。
- 2026-03-29 orgtree 闭环 46：完成“用户可访问模块配置”最小闭环，严格限定在模块访问层，不扩到按钮/字段/数据权限。后端新增 `sys_user_module_permission` 及 `UserModulePermissionController/Service/Mapper`，提供模块定义列表、用户授权查询/整批覆盖保存、当前登录用户模块列表四个最小接口；超级管理员继续按现有 RBAC Lite 视为全量可访问，不影响组织架构展示层对超级管理员的既有过滤规则。前端在 `OrgTreeView.vue` 为选中用户补了“配置可访问模块”弹窗，支持模块列表加载、已有授权回显、整批保存；同时把侧边菜单抽到统一模块常量，按当前用户 `moduleCodes` 过滤菜单，仅保留首页/个人中心作为基础页兜底，并在路由守卫中对未授权模块做最小 `403` 拦截，避免手输未授权路由直接进入页面。验证结果：`mvn -DskipTests compile` 通过；`nvm use 20 >/dev/null && npm run build` 通过；真实启动 `DB_HOST=172.24.16.1 mvn -Dspring-boot.run.profiles=wsl spring-boot:run` 成功；真实接口联调验证了 `/api/auth/login`、`/api/auth/me`、模块定义查询、用户 3 的模块授权保存/回显、数据库记录落库，以及被授权用户 `orguser1` 登录后 `/api/user-module-permissions/current` 返回 `attendance/statistics/weeklywork`。联调过程中还用现有管理员重置密码链路把 `orguser1` 密码恢复为默认 `123456` 后完成验证。
- 2026-03-27 weeklywork 闭环 45：按页面使用反馈继续收口周报三分区交互，只处理前端体验问题，不改后端审批链。`WeeklyWorkEditView.vue` 本轮补了三项：1）详情弹层头部统一补“关闭”按钮，因此“我的填报”里点击查看详情后也有了明确关闭入口；2）审核分区显式把默认展开规则收口为“待我审核默认展开、我已审核默认收起”，避免旧状态残留导致已审核列表一上来就整段展开；3）在“待我审核 / 我已审核”列表上方新增查询条，支持按周次、填报人、任务概览、当前阶段和退回摘要筛当前分区记录，并补命中数/总数摘要与清空搜索入口。这样周报审核与回看两端都更接近日常操作路径：能查、能收、也能随时关详情。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 param 闭环 44：完成“参数页筛选工作面前移与空态回退”最小闭环，只处理前端筛选上下文，不改接口。`ParamListView.vue` 现已在参数列表区顶部补充“当前筛选工作面”摘要，统一展示当前关键词与状态筛选；当列表被筛空时，空态里也会同步展示这条摘要，并在存在筛选条件时给出“清空当前筛选”入口。这样参数页查不到数据时，不再只是一个泛空态，而是能直接说明当前到底带着什么条件在查，也能一键恢复到默认列表。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 operationlog 闭环 43：完成“操作日志页查询工作面前移与空态回退”最小闭环，只处理前端查询上下文，不改接口。`OperationLogListView.vue` 现已在日志列表区顶部补充“当前查询工作面”摘要，统一展示当前模块、操作人和时间范围；当列表被筛空时，空态也会同步显示这条摘要，并在存在筛选条件时给出“清空当前查询条件”入口。这样操作日志页查不到数据时，不再只是一个泛空态，而是能直接说明“当前到底按什么条件在查”，也能一键回到默认日志列表。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 attendance 闭环 42：继续收口考勤页“快捷时间”和“当前范围提示”的一致性，只处理前端工作面表达，不改接口。`AttendanceCheckInView.vue` 现已在“当前查询工作面”摘要卡中补充“快捷时间”字段，会按当前日期范围自动显示“今天 / 近7天 / 本月 / 自定义”；这样用户看到时间范围时，不必再自己回头对照按钮判断当前是否仍处在某个快捷时间模式，顶部摘要与快捷按钮状态终于能用同一套口径表达当前时间工作面。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 attendance 闭环 41：继续收口考勤页“筛空后的回退动作”，只处理前端查询工作面的恢复入口，不改接口。`AttendanceCheckInView.vue` 现已把“清空当前查询条件”入口补到查询区的“当前查询工作面”摘要卡和列表空态里，并把日期范围、状态筛选、关键词、组织、异常筛选、异常用户联动、趋势日期联动全部纳入可回退条件判断。这样当考勤列表因带了状态/日期/关键词/联动而筛空时，用户不再只能依赖“恢复异常联动前筛选”，而是可以直接一键清空当前整个查询工作面，快速回到默认列表。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 attendance 闭环 40：完成“考勤页查询工作面前移展示”最小闭环，只处理前端上下文感知，不改接口。`AttendanceCheckInView.vue` 现已在查询区下方新增“当前查询工作面”摘要卡，统一展示当前时间范围、筛选条件和联动状态，并把“清除当前联动”入口前移到顶部。这样当页面因异常用户联动、趋势日期联动、关键词或组织筛选而进入特定工作面时，用户一抬头就能看到自己当前到底在看什么，不必再滚到考勤列表区底部才发现仍挂着联动或筛选条件。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 unit 闭环 39：继续收口单位页“打卡点设置”弹层的回退路径，只处理“当前改乱了坐标/半径/地址后虽然能看到差异，但没有一键恢复到已保存状态或初始状态”的最小前端问题。`UnitListView.vue` 现已在“待保存变更摘要”卡片中新增恢复按钮：若当前单位已有已保存打卡点，则显示“恢复到已保存”，一键把名称、半径、坐标、地址和状态全部恢复到最新已保存详情；若当前还没有已保存打卡点，则显示“恢复到初始状态”，回退到默认名称、默认半径和空选点状态。这样用户在调图调半径时即使走偏了，也能直接撤回到稳定状态，不必手动把字段一个个改回去。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 unit 闭环 38：继续收口单位页“打卡点设置”弹层的退出路径，只处理“当前改了坐标/半径/地址后点取消或点遮罩会直接关闭，未保存修改没有确认”的最小前端问题。`UnitListView.vue` 已将打卡点弹层改为受控显示，取消按钮与遮罩关闭统一走 `handleCloseLocationDialog`：若存在待保存变更，会先弹出“放弃修改”确认；只有用户确认后才真正关闭弹层，保存中则继续禁用关闭。这样单位页打卡点设置终于补上了未保存修改保护，不再因为误触关闭而直接丢掉刚调好的选点结果。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 unit 闭环 37：继续收口单位页“打卡点设置”弹层的回看与编辑链路，只处理“当前详情”和“当前选点”之间缺少差异摘要，用户改完坐标/半径/地址后还得自己上下比对字段”的最小前端问题。`UnitListView.vue` 现已在打卡点设置弹层中新增“待保存变更摘要”卡片，会自动对比已保存详情与当前表单/选点状态，按项提示打卡点名称、打卡半径、打卡坐标、打卡地址的变化；若当前未改动，也会明确提示“当前选点与已保存打卡点一致，暂时没有待保存变更”。这样用户在保存前可以直接看到这次到底改了哪些字段，不必再自己来回比对“当前打卡点详情”和“当前选点摘要”两块内容。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 unit 闭环 36：完成“单位页搜索筛空提示与系统空数据提示分离”最小闭环，只处理前端空态表达，不改接口。`UnitListView.vue` 现已补充搜索上下文感知：当列表有数据但被关键词筛空时，空态会明确显示“未找到与某关键词匹配的单位”，并给出“清除搜索词”按钮；当系统本身暂无单位数据时，则继续显示原来的新增引导。同步在筛选有结果时补了一行“当前关键词”摘要，避免用户忘记自己正在带条件浏览。这样单位页终于能区分“查无结果”和“系统没数据”两种完全不同的状态，搜索失败时也有明确回退动作。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 statistics 闭环 35：继续收口统计页的查询上下文感知，只处理“切换周次或组织后，各统计区本身没有明确提示当前口径，用户需要反复回看查询区确认”的最小前端问题。`StatisticsOverviewView.vue` 现已新增统一的 `currentScopeText`，把当前周次与组织筛选收口成一条明确说明，并在总体概览、组织排名、红牌名单、黄牌名单四个分区顶部直接展示；趋势区提示也同步复用这条口径文案，再叠加趋势截止周与展示范围说明。这样当前统计页五个主要分区都能直接说明自己“按什么周次、什么组织范围”展示结果，切换筛选后不必再回滚到顶部重新确认上下文。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 statistics 闭环 34：继续收口统计页趋势区的阅读顺序，只处理“后端按最新周倒序返回，但页面提示写成历史范围从旧到新，表格阅读方向和提示不一致”的最小前端问题，不改接口。`StatisticsOverviewView.vue` 现已将趋势表改为基于返回结果反转后按时间正序展示，并在趋势提示里明确补充“表格按时间正序展示”，这样用户阅读“最近4周趋势”时会从最早一周一路看到截止周，和范围说明保持一致，不再出现提示讲旧到新、表格却从新到旧的违和感。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 statistics 闭环 33：继续收口统计页概览展示口径，只处理“总体概览里把 `normalCount` 标成优秀人数、而 `yellowCount` 明明有数据却没展示”的最小前端问题，不改后端统计逻辑。`StatisticsOverviewView.vue` 现已把概览卡调整为“平均分 / 红牌人数 / 黄牌人数 / 正常人数 / 覆盖率”，其中原先误导性的“优秀人数”文案已改为更准确的“正常人数”，同时补出了原本被隐藏的黄牌人数卡片，确保页面概览与下方红黄牌名单、后端 `overview` 返回字段口径一致。验证结果：`nvm use 20 >/dev/null && npm run build` 通过；仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 statistics 闭环 32：完成“统计页趋势区与周次查询口径对齐”最小闭环，解决原先上方概览/排名/红黄牌会跟随 `weekNo` 变化，但“最近4周趋势”始终只按最新 4 周返回、与当前查询周次脱节的问题。后端 `StatisticsController / StatisticsServiceImpl / StatisticsMapper.xml` 已将 `/api/statistics/trend` 扩展为接收 `weekNo`，并改为只查询 `week_no <= 当前查询周次` 的最近 4 个周次；前端 `StatisticsOverviewView.vue` 现在会把当前 `weekNo` 一并传给趋势接口，并在趋势区顶部明确提示“当前按哪一周作为趋势截止周，以及实际展示范围”。这样用户把周次切回历史周时，下方趋势不再偷偷跳到最新周，而会围绕所选周次向前展示最近 4 周，页面五块统计口径终于一致。验证结果：`mvn -Dtest=StatisticsServiceImplTest test` 通过，`nvm use 20 >/dev/null && npm run build` 通过；前端仍有既有 Vite 大包体 warning，本轮未处理。
- 2026-03-27 weeklywork 闭环 31：继续收口周报审核分区在“最后一条处理完后”的回退路径，只处理“待我审核 / 我已审核 空态只有说明文案，没有快速回到我的填报入口”的最小问题，不改数据流和路由结构。前端 `WeeklyWorkEditView.vue` 在审核空态区补了“返回我的填报”按钮，直接复用现有 `handleRecordTabChange('mine')` 切回填报分区，这样当待审或已审列表被清空时，用户不必自己再去点顶部页签寻找出口，处理完最后一条即可顺手回到个人周报工作面。验证结果：使用 `nvm use 20 >/dev/null && npm run build` 构建通过；仍保留既有 Vite 大包体 warning。当前剩余主线依然是浏览器人工联调，重点确认真实审批后“顶部摘要 -> 详情 -> 空态 -> 返回我的填报”的整条回退路径是否自然。
- 2026-03-27 weeklywork 闭环 30：继续收口周报审核分区顶部摘要与详情之间的直达链路，只处理“待我审核 / 我已审核 顶部摘要卡没有直接操作入口，尤其已审核分区默认收起列表后还要再下滑展开”的最小问题，不改后端和列表数据结构。前端 `WeeklyWorkEditView.vue` 在审核摘要卡底部新增主按钮，复用现有卡片主动作：待我审核直接“进入审核”，我已审核直接“查看记录”，从顶部当前聚焦摘要即可一步进入详情，无需先滚到下方记录区再展开列表；这样和上一闭环补的空态关闭按钮连起来后，审核分区的“看摘要 -> 开详情 -> 关闭回看”路径更顺。验证结果：使用 `nvm use 20 >/dev/null && npm run build` 构建通过；仍保留既有 Vite 大包体 warning。当前剩余主线仍是浏览器人工联调，重点看真实审批后顶部摘要、折叠列表与详情弹层三者的切换是否足够自然。
- 2026-03-27 weeklywork 闭环 29：继续收口周报审核分区的焦点回落体感，只处理“待我审核 / 我已审核 在切到空焦点时详情弹层无显式退出动作”的最小问题，不改后端链路。前端 `WeeklyWorkEditView.vue` 在审核分区详情空态中补了明确的“关闭”按钮，这样当审批通过、退回或切分区后当前分区已无可聚焦记录时，用户不必依赖遮罩点击或系统自动回收，也能直接退出底部详情弹层，避免页面停在“当前没有可查看的周报详情”的悬空状态。验证结果：使用 `nvm use 20 >/dev/null && npm run build` 构建通过；仍有既有的 Vite 大包体 warning，但不影响本轮功能闭环。当前剩余主线可继续围绕真实浏览器人工联调，重点确认待我审核 / 我已审核 分区在连续审批后的自动聚焦、展开/收起与回看体感。
- 2026-03-27 weeklywork 闭环 28：在闭环 27 的真实环境基础上继续补周报审批往返联调，围绕 `weekly_work.id=8` 跑通了一次完整链路。先由 `wz(王哲)` 对 `PENDING_SECTION_CHIEF` 记录执行 `APPROVE`，接口与数据库同步推进到 `PENDING_DEPUTY_LEADER / USER_12`，并新增 `USER_13 -> USER_12` 审批日志；随后由 `zy(张琰)` 执行 `RETURN` 到 `STAFF` 并填写“请补充团体任务说明”，接口详情与数据库均正确回落到 `RETURNED / STAFF / current_handler_user_id=15`，`last_return_target/last_return_comment/last_review_by` 也全部正确写入；最后由填报人 `hnj(黄能建)` 调 `submit` 重新提交，记录重新回到 `PENDING_SECTION_CHIEF / USER_13`，`submit_time` 更新到本次重提时间，`last_return_*` 与 `last_review_*` 被清空，说明“退回重提”主链已恢复可用。联调中还补测了负向场景：`wjf(吴进法)` 试图审批当前挂在 `wz` 手上的 `id=8`，接口明确返回“当前用户不是该周报的审批人”。说明：为继续联调，测试账号 `zy/hnj` 的密码曾通过数据库对齐到已验证可登录的 hash；这是临时测试手段，不是功能代码修复。当前剩余更值得继续收口的是前端页面里待我审核/我已审核分区在这些真实流转后的展示与焦点回落体感。
- 2026-03-27 weeklywork 闭环 27：继续按真实环境联调周报审批链，先按 `start.txt` 启动 Windows 侧 PostgreSQL 并确认 WSL 到宿主机数据源应使用 `172.24.16.1:5432`，随后以 `mvn -Dspring-boot.run.profiles=wsl -Dspring-boot.run.jvmArguments="-DDB_HOST=172.24.16.1" spring-boot:run` 重新拉起后端。联调过程中发现当前项目编译未启用 `-parameters`，导致显式依赖参数名推断的接口会在运行时报 `Name for argument of type [...] not specified`；本轮对 `WeeklyWorkController.detail` 与 `UserController` 中 `detail/update/delete/resetPassword` 的 `@PathVariable` 全部改为显式命名，绕开编译参数依赖。随后使用管理员账号重置真实审批链账号口令，完成一次真实最终审批通过验证：`weekly_work.id=5` 由 `wjf(吴进法)` 执行 `APPROVE` 后，数据库中状态更新为 `APPROVED`，`current_handler_user_id/current_approval_node` 清空，`approved_time` 与 `last_review_by=10` 正确写入，`weekly_work_approval_log` 新增 `USER_10 -> APPROVED` 日志；接口 `GET /api/weekly-work/detail/5` 也已返回最新审批结果与新增日志。当前剩余待联调主线可继续转到退回重提、非当前审批人拒绝审批，以及待我审核列表在前端页面上的实际回落表现。
- 2026-03-25 weeklywork 闭环 26：只围绕“审批提交后状态不变、按钮不消失”的审批链路失败问题做最小修复，不扩展新模块。定位结果是后端 `weekly_work` 表字符串字段长度不足，审批推进时 `status/current_approval_node/last_return_target` 被写入更长的真实节点编码后触发 PostgreSQL `value too long for type character varying(20)`，导致 `markSubmitted/updateApproval` 整条 SQL 回滚，前端看到的就只是状态没变、按钮仍存在。本轮新增 PostgreSQL migration `backend/src/main/resources/sql/20260325_weekly_work_column_lengths.sql`，把 `status` 扩到 `varchar(50)`、`current_approval_node` 和 `last_return_target` 扩到 `varchar(100)`；同时同步修正初始化脚本 `backend/database/14_weekly_work_approval_flow.sql` 中审批节点字段长度，并保留 `last_review_by` 为数值型，避免回退到先前已修过的类型错误。后端 `WeeklyWorkServiceImpl` 里给 `submit` 和 `review` 增加了受影响行数校验，若 `markSubmitted/updateApproval/insertApprovalLog` 未成功落一行，会明确抛出“提交周报失败 / 更新审批状态失败 / 写入审批日志失败”，避免数据库异常时前端只表现为“点了没反应”。前端当前项目没有 `WeeklyWorkReviewDialog.vue`，审批入口实际集中在 `WeeklyWorkEditView.vue`，因此本轮继续在现有页面收口：编辑态组合提交成功提示改为“已保存并提交到下一步”，审批通过/退回失败提示更明确，成功后仍复用现有关闭详情并刷新状态的链路。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 通过，`npm run build` 通过。说明：本项目当前真实状态模型仍是 `PENDING_SECTION_CHIEF/.../APPROVED`，本轮没有强行改成 `PENDING_REVIEW/FINISHED`，以免扩大联动范围。
- 2026-03-25 weeklywork 闭环 25：按“只做任务编辑态直接提交到下一步的最小前端闭环”继续收口，不改后端接口、数据库和审批规则。核对 `WeeklyWorkEditView.vue` 后确认现有 `handleSubmitCurrent` 已采用“先 `handleSaveDraft({ silentSuccess: true, failureMessage: '保存失败，无法提交到下一步' })`，保存成功后再调 `submitWeeklyWork`” 的组合链路，且提交成功后已通过 `refreshWorkflowState(targetId, { openDetail: false, syncCurrent: true, tabKey: activeRecordTab.value })` 退出编辑态并刷新当前周报状态，提交失败则仅提示错误并停留编辑态。本轮补齐的真实缺口是审批修改态底部此前只显示“提交到下一步”而未保留“保存草稿”，因此把 `showSaveButton` 放宽到 `create/edit/approval_edit` 三种编辑态，确保普通编辑、退回修改、审批修改三类编辑场景统一保留“保存草稿 / 提交到下一步”两个主按钮，顶部继续仅弱化保留“退出编辑/返回列表”，不再要求用户先退回主页面手动提交。验证结果：`npm run build` 通过；后端本轮未改，未额外执行后端编译，仅说明未涉及。当前代码层已满足编辑态下一步完成提交的最小前端目标，剩余仅是浏览器人工补点确认保存失败、提交失败和提交成功后的实际页面体感。
- 2026-03-24 weeklywork 闭环 24：只围绕“任务编辑态直接提交到下一步”做最小前端交互优化，不改后端接口与审批规则。`WeeklyWorkEditView.vue` 中编辑态底部主按钮文案统一为“保存草稿 / 提交到下一步”，其中 `handleSubmitCurrent` 不再要求用户先退出到主页面再提交，而是改为固定先调用 `handleSaveDraft({ silentSuccess: true })` 保存当前编辑内容；只有保存成功后才继续调用现有 `submitWeeklyWork`，保存失败会直接中断提交流程并提示“保存失败，无法提交到下一步”。提交成功后则复用现有状态刷新链路，执行 `refreshWorkflowState(targetId, { openDetail: false, syncCurrent: true, tabKey: activeRecordTab.value })`，从编辑态退回到列表/查看态并刷新当前周报状态；如果提交失败，则停留在编辑态，仅提示失败原因，不会错误退出。同步把审批修改态提示文案也改成“可直接点击底部‘提交到下一步’”，避免继续暗示“先保存后回主页面再提交”。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过。当前代码层已满足手机端在编辑态下一步完成提交的目标，下一步只需浏览器人工补点创建新周报、编辑已有周报、退回后修改重提三条真实点击体验。
- 2026-03-24 weeklywork 闭环 23：只围绕“周报页底部操作区极简化”做最小前端交互收口，不改后端。`WeeklyWorkEditView.vue` 的底部常驻操作区继续只在“我的填报”编辑相关态出现，但按钮收成尽可能简单的移动端组合：底部移除原来和主按钮同级的“退出编辑”，只保留两个主动作“保存草稿 / 提交审核”；其中“保存草稿”沿用现有草稿保存链路，原“暂存”文案同步改为“保存草稿”。“退出编辑”则从底部大按钮弱化到任务区顶部次要按钮，只有真正进入编辑态或审批修改态时才出现；非编辑态继续只保留卡片区的“查看 / 继续编辑 / 提交审核”入口，不再在底部重复强调编辑类操作。这样手机端底部主操作更聚焦，用户一眼只需判断“保存”还是“提交”，不再被第三个大按钮分散注意力。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过。当前代码层已完成底部操作区极简化，下一步只需浏览器人工补点编辑态与查看态切换时的实际体感。
- 2026-03-24 weeklywork 闭环 22：只围绕“审核提交成功后页面仍停留在旧审核态”做最小前端交互收口，不改后端。`WeeklyWorkEditView.vue` 中审批通过与退回成功后的链路统一改为“先关闭当前审核详情页，再刷新列表并回落焦点”：`handleReview` 与 `confirmReturnFromSheet` 成功后不再以 `openDetail: true` 继续停留在详情页，而是调用 `refreshWorkflowState(..., { openDetail: false, syncCurrent: false, excludeId: 当前记录 })`；同时 `refreshWorkflowState` 在 `openDetail = false` 时会先执行 `clearDetailContext()`，立即清空旧详情和审核操作区，再重拉列表并把焦点落到当前分区的下一条记录，没有下一条则自然进入空态。现有按钮禁用态继续复用 `reviewTargetId/reviewAction`，因此点击“提交下一级”或“退回上一级”后按钮会先进入 loading/禁用，失败时页面不会关闭，按钮会在 finally 中恢复并提示失败原因。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过。当前代码层已经满足“成功即退出审核页、回到列表、回落下一条/空态；失败则保留页面”的最小闭环，下一步只需浏览器人工补点通过与退回两条真实点击体验。
- 2026-03-24 weeklywork 闭环 21：只围绕“审批动作完成后按钮仍残留”做最小前端权限收口，不改后端接口结构。`WeeklyWorkEditView.vue` 把审批权限判断改为“审批快照字段优先、旧节点逻辑兜底”：新增 `hasApprovalSnapshot`、`resolveCurrentHandlerUserId`、`isWorkflowApproved`，`canReview` 现优先基于 `current_handler_user_id` 与 `approved_time` 判断当前处理权，只有在旧数据未带审批快照时才回退到 `currentApprovalNode/flowNodes` 推断，因此提交下一级成功后原审批人会立即失去审核权限、退回后原审批人也会立即失去审核权限，最终审批通过后则因 `approved_time` 已写入、`current_handler_user_id` 已清空而统一判定为已完成且不再显示审批按钮。同步把 `statusLabel`、`resolveCurrentApprovalNode`、`currentStageText`、`nextFlowStepText`、`currentFlowTargetText` 与流程节点状态里的“已完成”判断一并切到 `approved_time` 兜底，保证顶部摘要、列表卡片、详情流程节点三处表达一致。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过。当前代码层已经把“提交下一级 / 退回上一级 / 最终审批通过”后三类按钮残留问题收口到同一套快照权限逻辑，下一步只需浏览器人工补点确认待我审核分区与详情动作区的最终页面表现。
- 2026-03-24 weeklywork 闭环 20：修复因运行库未执行增列 SQL 导致的 `weekly_work.current_handler_user_id 不存在` 查询报错。本轮未再改业务代码，直接将 `backend/src/main/resources/sql/20260324_weekly_work_snapshot_fields.sql` 应用到当前运行库 `lecture_team.public`，`ALTER TABLE` 已成功补齐 `current_handler_user_id`、`current_handler_user_name`、`current_flow_order`、`final_approver_user_id`、`approved_time`，其中 `last_review_by_name` 原库已存在因此被安全跳过。随后核对 `information_schema.columns`，确认 `weekly_work` 现已具备全部审批快照字段；验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过，`npm run build` 通过。当前这类错误的直接根因不是 Mapper 语法，而是代码版本已升级但运行库结构尚未同步。
- 2026-03-24 weeklywork 闭环 19：只围绕周报审批页面防回归做最小修复，不改后端接口。`WeeklyWorkEditView.vue` 里把审批日志读取收口为统一 `extractApprovalLogs` / `normalizeWeeklyWorkRecord` helper，兼容 `approvalLogs / approvalRecords / approvalLogList / approvalRecordList / logs / logList` 等字段名，并且只有在接口真实返回日志字段时才挂到记录对象上，避免列表项空数组在详情同步时把已有审批日志覆盖掉；详情时间线、最近退回推导都改为走这套 helper，因此过渡态、空态和焦点同步不再误伤日志区。流程节点展示则继续统一走 `buildFlowNodes`，并增强 `shouldHideFlowDisplayNode`：优先按 `roleCode === 'SUPER_ADMIN'` 过滤，同时兼容 `key / aliases / name / label / displayLabel / nodeLabel` 中出现“超级管理员”的情况；`approvalNodeLabel / currentStageText / returnTargetText` 也优先基于过滤后的显示节点数组取值，保证顶部阶段条、审批链摘要、详情流程节点和时间线节点名称一致地排除超级管理员。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过。当前代码层防回归点已补齐，但仍需浏览器人工补点确认详情页审批日志与三处流程展示的最终页面表现。
- 2026-03-24 weeklywork 闭环 18：围绕 `weekly_work` 主表补齐审批快照字段，最小新增 `current_handler_user_id`、`current_handler_user_name`、`current_flow_order`、`final_approver_user_id`、`approved_time`，并补充伴随字段 `last_review_by_name`，避免再把姓名写入 bigint 字段。后端 `WeeklyWorkServiceImpl` 现已在提交、审批通过、退回三条主链上统一维护这些字段：提交时如存在审批链则写入当前处理人、当前流程序号和最终审批人，如无审批链则直接 `APPROVED` 并写 `approved_time`；审批通过时若还有下一节点则切换到下一处理人并推进序号，若到最终节点则清空 `current_handler_*`、保留最终流程序号并写 `approved_time`；审批退回时则把 `current_handler_*` 和 `current_flow_order` 回写到退回目标。同步更新了 `WeeklyWorkEntity`、`WeeklyWorkDetailVO`、`WeeklyWorkListItemVO`、`WeeklyWorkMapper.xml` 和最小 SQL 变更文件 `backend/src/main/resources/sql/20260324_weekly_work_snapshot_fields.sql`，并让单测桩映射一起跟上。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 11 项通过，`npm run build` 通过。当前代码层验证样本为：提交到首级审批时 `current_handler_user_id=2`、`current_handler_user_name=用户2`、`current_flow_order=2`、`final_approver_user_id=2`、`approved_time=NULL`；最终审批通过后 `status=APPROVED`、`current_handler_* = NULL`、`current_flow_order=2`、`final_approver_user_id=2`、`approved_time!=NULL`；退回到填报人时 `status=RETURNED`、`current_handler_user_id=3`、`current_handler_user_name=用户3`、`current_flow_order=1`、`final_approver_user_id=2`、`approved_time=NULL`、`last_review_by=2`、`last_review_by_name=测试用户`。
- 2026-03-24 weeklywork 闭环 17：修复审批通过/退回时 `weekly_work.last_review_by` 的类型错误，不改前端权限逻辑。定位结果为：数据库字段 `last_review_by` 是 `bigint`，但后端 `WeeklyWorkEntity.lastReviewBy` 误定义为 `String`，`WeeklyWorkServiceImpl.review()` 又把审批人姓名 `resolveDisplayName(loginUser)` 写入该字段，导致 `updateApproval` 执行时报 “bigint vs character varying” 并整条审批 SQL 失败。当前已将 `WeeklyWorkEntity.lastReviewBy` 与 `WeeklyWorkDetailVO.lastReviewBy` 统一改为 `Long`，并把 `review()` 中的赋值修正为 `loginUser.getUserId()`；同时补充单测断言，确认退回后 `lastReviewBy=2L` 被正确写回。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，`npm run build` 通过。由于审批状态现在能真实落库，前端刷新后“待我审核”归属和审批按钮显隐会自动按真实状态收口。
- 2026-03-24 weeklywork 闭环 16：只围绕“任务内容换行未显示”做最小前端展示修复，不改后端。`WeeklyWorkEditView.vue` 中列表卡片的“任务概览”、详情里的任务时间/进展/协助文本，以及我自己任务卡片正文统一接入保留换行的展示样式，使用 `white-space: pre-wrap` 与 `word-break: break-word`，让已有换行按多行显示，同时长文本也能自动断行而不撑破卡片或详情布局；未改输入框和后端接口，也没有使用 `v-html`。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。
- 2026-03-24 weeklywork 闭环 15：只围绕“查看周报后缺少明确退出路径”做最小前端收口，不改后端。`WeeklyWorkEditView.vue` 在“我的填报”顶部查看态补了“返回当前分区”按钮，命中当前 `view` 模式且有选中周报时即可直接退出查看并回到当前分区默认状态；同时在详情弹层头部补了显式“关闭”按钮，复用现有 `closeDetail` 清理详情上下文，保证无论是顶部查看态还是详情弹层查看态都始终存在明确出口。退出后保持原分区不变，不残留旧详情弹层或顶部查看态标题。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。
- 2026-03-24 weeklywork 闭环 14：只收口两个前端展示问题，不改后端。`WeeklyWorkEditView.vue` 中“当前周报任务列表”右上角新增按钮改为仅在 `taskList.length > 0` 时显示，空任务时只保留空态卡片里的“新增第一项任务”入口，避免同屏出现两个新增入口；审批链展示继续沿用现有 `flowNodes` / 组织架构动态生成，但在最终展示节点数组上统一过滤掉 `roleCode === 'SUPER_ADMIN'` 或 `name === '超级管理员'` 的节点，因此顶部流程阶段条、详情流程节点、审批链摘要和节点状态摘要都会自动排除超级管理员。验证结果：`npm run build` 通过。
- 2026-03-24 weeklywork 闭环 13：本轮不再继续改 UI，只补人工验收辅助能力。`WeeklyWorkEditView.vue` 新增仅开发模式生效的统一焦点调试日志方法，输出格式收口为 `[WeeklyWorkFocus] event=..., tab=..., focusId=..., stage=..., nextStep=..., returnTarget=..., transition=...`；并在分区切换、打开新详情、审批通过、审批退回、审批后焦点回落等关键操作点打印日志，方便浏览器人工走查时直接对照当前分区、焦点记录、当前阶段、下一步、退回目标和 transition 状态定位问题。日志通过 `if (import.meta.env.DEV)` 控制，只在开发环境输出，不影响生产页面表现。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。
- 2026-03-24 weeklywork 闭环 12：继续围绕“真实点击时的旧内容闪现、焦点跳变与空态生硬”做最小前端微收口，`WeeklyWorkEditView.vue` 新增统一的审核分区切换占位：审批通过、审批退回、审核分区切换记录或详情加载时，顶部审核摘要、当前查看提示和详情弹层会先进入同一套“切换中”占位，再落到新的焦点记录或空态，避免旧详情和旧顶部摘要短暂闪回；`openDetailById` 在切换到新记录前会先清空旧 `detailRecord`，`refreshWorkflowState` 在审批动作完成后也会先拉起占位再重算焦点，没有下一条时则直接保留明确空详情占位而不是残留旧上下文。同时补充详情加载说明、小屏下流程节点横向滚动 `scroll-snap` 与更稳的描述行高，进一步减少手机上流程条和焦点提示的拥挤跳动。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。仍需浏览器人工补点审批通过后切下一条、退回后分区变空，以及真机小屏横向滚动的最终体感。
- 2026-03-24 weeklywork 闭环 11：继续围绕“人工浏览器走查时的焦点/空态/过渡稳定性”做最小前端收口，`WeeklyWorkEditView.vue` 新增统一的分区焦点选择规则：切换 `我的填报 / 待我审核 / 我已审核` 时会优先为当前分区选择首条可用记录，审批通过或退回后则自动把焦点回落到当前分区下一条记录；若当前分区已无可聚焦记录，则主动清空旧详情并落到明确空态，不再残留上一条记录的上下文。详情弹层补充“当前没有可查看的周报详情”占位，记录区焦点提示条继续与顶部、列表保持一致；同时小屏下进一步压缩焦点提示和空态文案字号，减少拥挤感。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。仍需浏览器人工补点真实审批通过/退回后的焦点回落体感，以及分区切换时顶部是否完全没有旧上下文闪现。
- 2026-03-24 weeklywork 闭环 10：围绕“人工浏览器验收体感”继续做最小前端收口，`WeeklyWorkEditView.vue` 新增分区下方的“当前查看记录”提示条，切换 `我的填报 / 待我审核 / 我已审核` 时会明确说明当前正在查看或回看的记录与当前模式，减少顶部切换时的认知跳变；审核分区补上默认首条记录聚焦与更强的卡片选中态，并在当前卡片标题旁显式标记“当前查看 / 当前回看”；同时把小屏布局进一步压缩，三分区标签改为纵向堆叠，顶部重点信息、阶段条、审核摘要网格统一改为单列，流程节点横向滚动宽度也放宽，降低手机上换行混乱和难读的问题。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。仍需浏览器人工补点分区切换动画体感、审核分区连续点多条记录时的视觉聚焦确认，以及真机小屏阅读体验。
- 2026-03-24 weeklywork 闭环 9：继续围绕“填报模式 / 审核模式进一步分离”做最小前端收口，`WeeklyWorkEditView.vue` 的顶部状态区改为按分区走双模式数据源：`我的填报` 继续绑定当前填报周报与编辑状态，`待我审核 / 我已审核` 则切换为审核模式，顶部默认展示审核说明与当前选中记录摘要，不再默认呈现填报编辑区；底部粘性填报操作条也只在“我的填报”显示，进一步消除“正在编辑他人周报”的错觉。同时把“当前阶段 / 下一步 / 退回目标”提升为顶部重点信息，并同步补到列表卡片和详情摘要中，保持与现有 `flowNodes/currentApprovalNode/approvalLogs/status` 同一套状态体系。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。仍需浏览器人工补点分区切换后的顶部体感、审核分区连续看多条记录时的认知清晰度，以及小屏下顶部三列摘要的可读性。
- 2026-03-24 weeklywork 闭环 8：继续围绕“填报与审核分区收口”做最小前端交互整理，`WeeklyWorkEditView.vue` 进一步把记录卡片点击行为按分区拆开：`我的填报` 仍可点击卡片同步切换顶部填报上下文，`待我审核 / 我已审核` 改为直接打开详情，不再把顶部填报编辑区切到他人周报，避免审核记录反向污染填报区；同时在列表卡片区与详情页补充统一的流程状态图例，把 `已处理 / 当前阶段 / 待处理 / 已退回 / 已完成` 的颜色和含义显式摆出来，配合现有流程节点状态高亮，人工验收时更容易对齐当前阶段。验证结果：`npm run build` 通过，`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过。仍需浏览器人工补点三标签切换后的实际体感与小屏流程节点可读性。
- 2026-03-24 weeklywork 闭环 7：围绕“提交周报时报 PostgreSQL 值太长(20)”先做双向定位，不再盲猜字段。后端 `WeeklyWorkServiceImpl.submit` 与审批日志写入前补充精确日志，打印 `weekNo/status/currentApprovalNode` 及其长度，并输出运行时实际连接的 `dbUrl/dbName/schema`；同时核对 `lecture_team.public.weekly_work` 与 `weekly_work_approval_log` 表结构，确认本次提交流程真实运行库中并不存在 `status/current_approval_node/last_return_target/last_return_comment varchar(20)`，且对 `markSubmitted` 的同款 SQL 进行了数据库回滚验证。最终通过真实 HTTP 调用完成一次“保存草稿 -> 提交周报”联调，`id=6` 成功提交为 `APPROVED`，提交日志显示运行时连接 `jdbc:postgresql://172.24.16.1:5432/lecture_team`，`weekNo='2026-W14-A1'` 长度 11、`status='APPROVED'` 长度 8、`currentApprovalNode=null` 长度 0，链路恢复可用。同步修正 `start.txt`，把后端启动命令统一为 `mvn -Dspring-boot.run.profiles=wsl spring-boot:run`，避免再跑到错误数据源环境。
- 2026-03-24 weeklywork 闭环 6：围绕“填报与审核动作混在一起、按钮重复、当前流程阶段不直观”做最小交互重构，`WeeklyWorkEditView.vue` 的记录区改为“我的填报 / 待我审核 / 我已审核”三分区标签页；“我的填报”只保留 `查看 / 继续编辑 / 提交审核`，“待我审核 / 我已审核”只保留审核视角入口，详情页审批动作统一为 `提交下一级 / 退回上一级 / 审批完成` 文案；列表卡片与详情页同时改为可视化流程节点并高亮当前阶段。为保证“我已审核”分区真实准确，后端 `WeeklyWorkListItemVO` 补充 `reviewedByCurrentUser`，服务层基于审批日志最小回填该布尔值。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，`npm run build` 通过。仍需浏览器人工补点三分区切换、待我审核详情动作区与最终节点“审批完成”的点击体验。
- 2026-03-24 weeklywork 闭环 5：只围绕审批操作后的页面状态同步收口，`WeeklyWorkEditView.vue` 新增统一的 `refreshWorkflowState` 刷新链路，提交/审批通过/退回/再次提交后统一强制重拉列表与详情，不再被“当前详情已打开”短路；同时去掉提交后前端先写死 `SECTION_CHIEF` 的假状态，改为完全以后端返回的真实 `status/currentApprovalNode` 为准，并把“最近退回”提示收口为仅在当前仍处于退回态时显示，避免重提后残留旧退回摘要、按钮和状态错觉。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，`npm run build` 通过。仍需浏览器人工补点提交、逐级通过、退回重提与非当前审批人进入详情的最终体验验收。
- 2026-03-23 weeklywork 闭环 4：周报列表与详情页补齐审批链节点的显式状态说明，新增“节点状态”摘要，把 `已处理 / 当前审批 / 待审批 / 已退回至此 / 待提交 / 已完成` 收口到同一套前端文案；同时把当前流转目标统一为“流程已完成 / 已退回至 X 待修改 / X 当前待审批”，并把最近退回改成“由哪个节点退回到哪个节点”的表达，便于人工验收一眼看懂当前链路。验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，`npm run build` 通过。
- 2026-03-23 weeklywork 闭环 3：围绕“审批流程人工联调闭环”先收口一个最直接的页面阻塞点，`WeeklyWorkEditView.vue` 的详情页审批操作区现在只对当前审批人显示真实可执行的“审核通过 / 退回”动作，不再暴露会把审批人带入错误提交流程的“修改后通过”入口；本轮已再次启动后端服务核对登录链路，周报后端主链测试 `mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，前端构建 `npm run build` 通过。当前 CLI 环境与仓库能力仍不足以诚实标记为“已完成真实浏览器逐项点击验收”，后续仍需在浏览器里补做员工提交、逐级审批、退回重提和无审批链直完场景的人工走查。
- 2026-03-23 weeklywork 闭环 2：周报列表与详情接口补齐真实 `flowNodes`，前端展示改为优先使用后端返回的真实审批链，补齐当前流转目标、审批链摘要、节点状态文案，并修复已完成状态显示为 `已完成` 而不是空白；后端测试 `mvn -Dtest=WeeklyWorkServiceImplTest test` 10 项通过，前端构建 `npm run build` 通过。
- 2026-03-23 weeklywork 闭环 1：周报提交与审批流从写死岗位节点改为按组织架构父链动态落到真实审批用户，超管自动跳过，只有当前审批人可审核，前端待我审批列表同步按真实当前审批人收口，后端单测 `mvn -Dtest=WeeklyWorkServiceImplTest test` 与前端构建 `npm run build` 均通过。

- 2026-03-16 user 闭环 1：用户页在忙碌状态下禁用搜索输入，减少查询参数在请求中途漂移。
- 2026-03-16 user 闭环 2：用户页查询方法增加忙碌态短路，拦截键盘触发的重复查询。
- 2026-03-16 user 闭环 3：用户页重置筛选方法增加忙碌态短路，避免取消搜索时重复刷新列表。
- 2026-03-16 user 闭环 4：用户页翻页方法增加忙碌态短路，避免分页连续点击造成重复拉取。
- 2026-03-16 user 闭环 5：用户页详情刷新方法增加忙碌态短路，避免详情刷新并发触发。
- 2026-03-16 user 闭环 6：用户页新增弹层入口增加忙碌态短路，避免请求中途重复打开弹层。
- 2026-03-16 user 闭环 7：用户页保存方法增加忙碌态短路，避免重复提交创建或编辑请求。
- 2026-03-16 user 闭环 8：用户页删除方法增加忙碌态短路，避免重复进入删除确认与提交。
- 2026-03-16 user 闭环 9：用户页重置密码方法增加忙碌态短路，避免重复进入确认与提交。
- 2026-03-16 user 闭环 10：用户页复制方法增加忙碌态短路，减少加载中无关交互反馈。
- 2026-03-16 user 闭环 11：用户页列表拉取方法增加忙碌态短路，统一拦截重复刷新入口。
- 2026-03-16 user 闭环 12：用户页刷新按钮改走专用处理函数，避免点击事件对象误入列表查询逻辑。
- 2026-03-16 user 闭环 13：用户页详情入口统一改走快捷处理函数，收口标题点击、详情按钮和上一位/下一位。
- 2026-03-16 user 闭环 14：用户页详情关闭改走专用处理函数，统一忙碌态下的关闭拦截。
- 2026-03-16 user 闭环 15：用户页卡片标题在忙碌态显式弱化，降低“可点却无响应”的误解。
- 2026-03-16 user 闭环 16：用户页分页组件在忙碌态下显式禁用，减少翻页误触。
- 2026-03-16 user 闭环 17：用户页状态筛选下拉在忙碌态下显式禁用，减少加载中切换筛选。
- 2026-03-16 user 闭环 18：用户页详情刷新按钮禁用条件统一到 pageBusy，视觉反馈与函数保护保持一致。
- 2026-03-16 user 闭环 19：用户页详情重置密码按钮禁用条件统一到 pageBusy，减少状态判断分裂。
- 2026-03-16 user 闭环 20：用户页回到顶部方法增加忙碌态短路，补齐函数层保护。
- 2026-03-16 user 闭环 21：用户页搜索框失焦时自动去掉首尾空格，筛选展示与真实查询参数继续保持一致。
- 2026-03-16 user 闭环 22：用户页翻页方法在点击当前页码时直接短路，避免同页重复请求。
- 2026-03-16 user 闭环 23：用户页详情快捷入口在重复点击当前已打开用户时直接短路，减少无效详情重载。
- 2026-03-16 user 闭环 24：用户页详情弹层在忙碌态下禁止点遮罩关闭，避免请求中途误收起。
- 2026-03-16 user 闭环 25：用户页从详情或卡片进入编辑时先收起详情弹层，避免双层弹层叠加。
- 2026-03-16 user 闭环 26：用户页详情弹层补充详情丢失时的空态兜底，异常状态下仍可继续返回列表。
- 2026-03-16 user 闭环 27：用户表单弹层在保存中禁止点遮罩关闭，避免提交中途误关弹层。
- 2026-03-16 user 闭环 28：用户创建表单的单位ID输入在保存中禁用，减少提交时参数被改写。
- 2026-03-16 user 闭环 29：用户创建表单的账号输入在保存中禁用，减少提交时账号漂移。
- 2026-03-16 user 闭环 30：用户创建表单的密码输入在保存中禁用，减少提交时初始密码被改写。
- 2026-03-16 user 闭环 31：用户表单的姓名输入在保存中禁用，统一核心字段的提交期保护。
- 2026-03-16 user 闭环 32：用户表单的岗位输入在保存中禁用，减少保存过程中资料变动。
- 2026-03-16 user 闭环 33：用户表单的手机号输入在保存中禁用，统一联系方式字段的提交期保护。
- 2026-03-16 user 闭环 34：用户表单的状态入口在保存中禁用，避免提交时切换启停状态。
- 2026-03-16 user 闭环 35：用户表单的密码显隐按钮在保存中禁用，减少提交过程无关交互。
- 2026-03-16 user 闭环 36：用户表单状态选择改走专用打开方法，函数层补上保存态短路。
- 2026-03-16 user 闭环 37：用户表单状态确认方法增加保存态短路，避免提交过程中继续改状态。
- 2026-03-16 user 闭环 38：用户创建表单清空方法补上保存态与原始态短路，函数层和按钮态保持一致。
- 2026-03-16 user 闭环 39：用户编辑表单恢复原值方法补上保存态与原始态短路，减少无效重置。
- 2026-03-16 user 闭环 40：用户表单提交方法补上保存态短路，进一步拦截重复提交。
- 2026-03-16 user 闭环 41：用户表单关闭时自动收起状态选择器，避免弹层关闭后残留子弹层。
- 2026-03-16 user 闭环 42：用户表单关闭时自动恢复密码隐藏态，避免下一次打开沿用旧显隐状态。
- 2026-03-16 user 闭环 43：用户表单进入保存态时自动收起状态选择器，减少提交过程中悬挂选择器。
- 2026-03-16 user 闭环 44：用户删除动作接入 submitting 状态，整页忙碌态与删除提交过程重新对齐。
- 2026-03-16 user 闭环 45：用户清理详情时同步清空详情加载态，减少异常关闭后的忙碌残留。
- 2026-03-16 user 闭环 46：用户页在表单关闭后自动清空编辑上下文，避免下一次打开沿用旧用户数据。
- 2026-03-16 user 闭环 47：用户页新增弹层入口在已处于新增态时直接短路，避免误触重置未保存输入。
- 2026-03-16 user 闭环 48：用户页编辑弹层入口在已打开同一用户时直接短路，避免重复拉取同一详情。
- 2026-03-16 user 闭环 49：用户表单状态选择子弹层在保存中禁止点遮罩关闭，与父弹层保存态保持一致。
- 2026-03-16 user 闭环 50：用户表单主要文本字段失焦时统一去掉首尾空格，减少无效空白字符进入提交参数。
- 2026-03-16 user 闭环 51：用户页表单打开时锁定顶部刷新与新增入口，避免编辑中误触背景操作。
- 2026-03-16 user 闭环 52：用户页表单打开时锁定搜索与筛选控件，避免编辑中切换列表上下文。
- 2026-03-16 user 闭环 53：用户页表单打开时锁定列表卡片操作，减少编辑中误点详情、编辑、删除与重置密码。
- 2026-03-16 user 闭环 54：用户页表单打开时锁定分页入口，避免编辑过程中切页导致上下文漂移。
- 2026-03-16 user 闭环 55：用户页详情入口函数补上表单打开态短路，避免弹层打开期间继续拉取详情。
- 2026-03-17 user 闭环 56：用户页详情弹层打开时锁定页头刷新与新增入口，减少浏览详情期间的背景操作漂移。
- 2026-03-17 user 闭环 57：用户页详情弹层打开时锁定搜索与筛选控件，避免详情浏览过程中切换列表条件。
- 2026-03-17 user 闭环 58：用户页详情弹层打开时锁定列表卡片、空态入口与分页，避免详情上下文被背景列表操作打断。
- 2026-03-17 user 闭环 59：用户页编辑入口命中当前已加载详情时直接复用详情数据，减少同一用户的重复详情请求。
- 2026-03-17 user 闭环 60：用户页从详情进入编辑后在取消关闭或保存成功时自动回到该用户详情，保持连续验收上下文。
- 2026-03-17 user 闭环 61：用户页刷新方法补上详情打开态短路，函数层与页头禁用状态重新对齐。
- 2026-03-17 user 闭环 62：用户页查询方法补上详情打开态短路，函数层与筛选区禁用状态保持一致。
- 2026-03-17 user 闭环 63：用户页重置筛选方法补上详情打开态短路，避免详情浏览期间重置列表条件。
- 2026-03-17 user 闭环 64：用户页分页方法补上详情打开态短路，函数层与分页禁用状态继续保持一致。
- 2026-03-17 user 闭环 65：用户页新增入口补上详情打开态短路，函数层与页头禁用状态完全对齐。
- 2026-03-17 user 闭环 66：用户页详情失效空态的刷新列表改走专用处理函数，避免详情态短路后出现“可点但不生效”。
- 2026-03-17 user 闭环 67：用户页详情区的回到列表顶部改为先收起详情再滚动，回列表动作与实际反馈一致。
- 2026-03-17 user 闭环 68：用户页详情快捷入口函数改用 listBlocked 短路，函数层与列表卡片禁用状态重新对齐。
- 2026-03-17 user 闭环 69：用户页编辑入口函数补上列表详情态短路，仅保留详情面板内的专用编辑入口可继续使用。
- 2026-03-17 user 闭环 70：用户页删除方法补上列表详情态短路，函数层与列表删除按钮禁用状态保持一致。
- 2026-03-17 user 闭环 71：用户页重置密码方法区分列表入口与详情入口，列表随详情态锁定，详情面板内仍可继续操作。
- 2026-03-17 user 闭环 72：用户页详情快捷入口支持区分详情面板内调用，修复“上一位 / 下一位”被列表短路误伤的问题。
- 2026-03-17 user 闭环 73：用户页详情上一位按钮在无邻近用户时改显“已到首位”，边界反馈更直接。
- 2026-03-17 user 闭环 74：用户页详情下一位按钮在无邻近用户时改显“已到末位”，边界反馈与上一位保持对称。
- 2026-03-17 user 闭环 75：用户页详情摘要新增本页首位/末位提示，连续翻阅时更容易判断当前位置边界。
- 2026-03-17 user 闭环 76：用户页详情快捷切换与列表快捷切换改走分流短路，详情浏览与列表锁定边界重新厘清。
- 2026-03-17 user 闭环 77：用户页详情连续浏览提示支持单人页文案分流，避免仅一人时仍提示可切换上一位/下一位。
- 2026-03-17 user 闭环 78：用户页详情摘要补充前后剩余可浏览人数，连续翻阅时更容易判断当前位置。
- 2026-03-17 user 闭环 79：用户页详情上一位按钮在可切换时直接显示目标用户，减少来回查看摘要的成本。
- 2026-03-17 user 闭环 80：用户页详情下一位按钮在可切换时直接显示目标用户，左右切换入口的信息密度保持一致。
- 2026-03-17 user 闭环 81：用户页详情标题补充“本页第几位 / 共几位”，顶部信息与摘要位置感保持一致。
- 2026-03-17 framework 闭环 82：新增后台主布局壳层 AppLayout，先提供统一侧栏/顶栏/内容区骨架。
- 2026-03-17 framework 闭环 83：后台主布局接入固定静态左侧菜单，先打通首页与现有模块入口。
- 2026-03-17 framework 闭环 84：后台主布局补齐顶部栏最小能力，支持当前页标题、登录人信息与退出登录。
- 2026-03-17 framework 闭环 85：新增首页欢迎页与通用页面容器，为登录后默认首页和后续模块挂载提供统一承载。
- 2026-03-17 framework 闭环 86：完成主框架路由挂接与守卫，登录后默认进首页，未登录访问业务页回跳登录，并补齐 404 页面。
- 2026-03-17 user 闭环 87：用户页接入通用页面容器 AppPageShell，正式作为后台壳层内的模板模块承载。
- 2026-03-17 user 闭环 88：用户页刷新与新增入口迁入页面容器 actions 区，模板页头结构与主框架保持一致。
- 2026-03-17 user 闭环 89：用户页补充模板模块说明提示，明确当前页承担后台模板页参考角色。
- 2026-03-17 user 闭环 90：移除用户页自带整页背景与高度占位，适配后台主框架内容区承载方式。
- 2026-03-17 user 闭环 91：用户页新增模块状态摘要，把总数、分页与筛选信息收成模板页顶部统一说明。
- 2026-03-17 framework 闭环 92：单位管理页接入通用页面容器，先从 TODO 占位提升为可挂载的后台模板页。
- 2026-03-17 framework 闭环 93：参数管理页接入通用页面容器，统一到后台模板页承载方式。
- 2026-03-17 framework 闭环 94：操作日志页接入通用页面容器，补齐后台占位模块的统一模板外壳。
- 2026-03-17 framework 闭环 95：单位、参数、操作日志页正式接入后台路由，模板页可从主框架内部直接访问。
- 2026-03-17 framework 闭环 96：后台静态菜单补齐单位、参数与操作日志入口，模板页从壳层导航可见可达。
- 2026-03-17 framework 闭环 97：组织架构页接入通用页面容器，先完成复杂模块与后台壳层的统一承载。
- 2026-03-17 framework 闭环 98：组织架构页顶部刷新与新增下级入口迁入页面容器 actions 区，复杂模块页头结构与模板页保持一致。
- 2026-03-17 framework 闭环 99：签到管理页接入通用页面容器，并把上下班打卡入口迁入模板页 actions 区。
- 2026-03-17 framework 闭环 100：周报管理页接入通用页面容器，并把暂存/提交入口迁入模板页 actions 区。
- 2026-03-17 framework 闭环 101：统计总览页接入通用页面容器，补齐后台主框架剩余轻量入口的模板化挂载。
- 2026-03-17 unit 闭环 102：单位管理页接入组织树数据拉取，先基于现有组织树生成最小单位视图。
- 2026-03-17 unit 闭环 103：单位管理页补充单位数、挂接人数与未分配人数摘要，先建立顶部概览信息。
- 2026-03-17 unit 闭环 104：单位管理页补充按单位ID搜索能力，先支持从单位视图里快速过滤目标单位。
- 2026-03-17 unit 闭环 105：单位管理页补充单位卡片列表，展示挂接人数、层级摘要与负责人候选信息。
- 2026-03-17 unit 闭环 106：单位管理页补充选中单位详情区，支持查看单位成员清单与基础信息。
- 2026-03-17 unit 闭环 107：单位管理页补充搜索结果摘要，先把当前展示范围和搜索关键词说明清楚。
- 2026-03-17 unit 闭环 108：单位管理页在筛选结果变化时自动同步选中单位，避免右侧详情停留在旧上下文。
- 2026-03-17 unit 闭环 109：单位管理页成员清单按层级和账号排序，先看更靠上的成员，再看同层顺序。
- 2026-03-17 unit 闭环 110：单位管理页详情补充启用/停用人数摘要，先建立单位状态分布感知。
- 2026-03-17 unit 闭环 111：单位管理页补充跳转组织架构快捷入口，先把单位视图与现有组织维护模块串联起来。
- 2026-03-17 unit 闭环 112：单位管理页详情补充成员搜索，先支持按姓名或账号快速定位单位成员。
- 2026-03-17 unit 闭环 113：单位管理页详情补充成员搜索结果摘要，当前展示人数与成员关键词一眼可见。
- 2026-03-17 unit 闭环 114：单位管理页详情补充成员搜索空态，避免过滤后无结果时成员区直接留白。
- 2026-03-17 unit 闭环 115：单位管理页卡片补充启用/停用人数，先在列表侧建立单位状态分布感知。
- 2026-03-17 unit 闭环 116：单位管理页成员卡片补充启停状态，单位详情与单位列表的状态信息保持一致。
- 2026-03-17 unit 闭环 117：单位管理页负责人候选改为按根节点/最上层启用成员优先推断，减少简单遍历带来的误判。
- 2026-03-17 unit 闭环 118：单位详情补充负责人候选独立展示，减少只在卡片区查看负责人信息的往返成本。
- 2026-03-17 unit 闭环 119：单位详情补充最高层级成员人数摘要，帮助快速判断单位结构深度分布。
- 2026-03-17 unit 闭环 120：单位卡片为“未分配单位”补充待处理标识，先把异常单位数据显式提示出来。
- 2026-03-17 unit 闭环 121：未分配单位详情补充处理提示，明确引导到组织架构模块继续核对归属。
- 2026-03-17 unit 闭环 122：单位详情补充负责人候选跳转用户页入口，并为成员卡片补充按账号与状态联动到用户页的快捷入口。
- 2026-03-17 user 闭环 123：用户页支持从路由 query 读取关键词与状态，补齐单位页跳转到用户页的接收能力。
- 2026-03-17 unit 闭环 124：单位页跳转用户页时补充来源与单位ID标记，为跨模块回跳和来源提示打底。
- 2026-03-17 user 闭环 125：用户页补充来自单位页的来源提示条，跨模块带入筛选时上下文更清晰。
- 2026-03-17 user 闭环 126：用户页空态补充返回单位管理入口，单位页跳转后无结果时可直接回退。
- 2026-03-17 unit 闭环 127：单位页支持从路由 query 读取 unitId 并自动恢复选中单位，补齐跨模块回跳后的上下文落点。
- 2026-03-17 unit 闭环 128：单位页补充来源提示条，返回用户页后的单位上下文恢复状态可直接感知。
- 2026-03-17 unit 闭环 129：单位切换时自动清空成员搜索条件，避免旧单位筛选残留影响新单位详情判断。
- 2026-03-17 user 闭环 130：用户页返回单位管理入口改为携带来源单位ID回跳，减少跨模块往返后的定位丢失。
- 2026-03-17 user 闭环 131：用户详情面板补充返回来源单位入口，跨模块回跳不再依赖列表空态或顶部区域。
- 2026-03-17 unit 闭环 132：单位详情负责人候选跳转用户页时补充 userId，支持从单位页直达目标用户详情。
- 2026-03-17 unit 闭环 133：单位成员卡片跳转用户页时补充 userId，成员入口与负责人入口保持同样的直达详情能力。
- 2026-03-17 user 闭环 134：用户页支持从路由 query 读取 userId 并在列表加载后自动打开详情，补齐单位页直达目标用户的接收能力。
- 2026-03-17 user 闭环 135：用户页来源提示补充“已按目标用户直达详情”反馈，跨模块直达行为对当前页面状态更透明。
- 2026-03-17 user 闭环 136：用户详情失效空态补充返回来源单位入口，直达详情失败时仍可快速退回原单位上下文。
- 2026-03-17 user 闭环 137：用户页手动关闭详情时同步清理路由中的 userId，仅保留单位来源上下文，关闭行为与页面状态保持一致。
- 2026-03-17 user 闭环 138：用户页成功打开详情后同步回写当前 userId 到路由，连续浏览与详情切换时页面状态和地址栏保持一致。
- 2026-03-17 user 闭环 139：用户页来源提示支持展示当前直达用户姓名/账号，跨模块落点确认不再只停留在“已直达详情”的泛提示。
- 2026-03-17 user 闭环 140：用户详情“回到列表顶部”改为复用统一关闭逻辑，回到列表时同步清理直达详情路由状态。
- 2026-03-17 user 闭环 141：用户详情失效空态补充目标 userId 提示，直达详情失败时当前失效对象更明确。
- 2026-03-17 user 闭环 142：用户详情失效空态的“刷新列表”改为复用统一关闭逻辑，刷新前先清理直达详情路由状态。
- 2026-03-17 user 闭环 143：删除当前详情用户时改为复用统一关闭逻辑，删除后的详情状态与路由状态一起收口。
- 2026-03-17 user 闭环 144：用户详情摘要补充来源单位信息，进入详情后不必回看顶部提示条也能确认上下文来源。
- 2026-03-17 user 闭环 145：用户详情失效提示补充来源单位信息，直达详情失败时目标用户与来源单位一起明确展示。
- 2026-03-17 user 闭环 146：用户页顶部来源提示条补充返回来源单位入口，不进入详情也可直接回跳原单位上下文。
- 2026-03-17 user 闭环 147：用户详情弹层被任意方式关闭时统一清理路由中的 userId，补齐遮罩关闭等场景下的状态收口。
- 2026-03-17 user 闭环 148：用户页焦点提示与空态补充来源单位信息，跨模块进入后的当前上下文在详情内外都更清晰。
- 2026-03-17 unit 闭环 149：单位页来源提示条补充返回用户管理入口，用户页与单位页之间的往返路径在两端都可见可达。
- 2026-03-17 user 闭环 150：用户页新增入口默认单位支持回退到来源单位上下文，未打开详情时也能延续单位来源做新增。
- 2026-03-17 unit 闭环 151：单位详情区补充“已恢复到来源单位”提示，回到单位页后当前选中状态在详情内也能直接确认。
- 2026-03-17 unit 闭环 152：单位页搜索无结果空态补充来源单位提示，跨模块回跳后即使筛空也不丢当前上下文。
- 2026-03-17 unit 闭环 153：单位页筛选摘要补充来源单位信息，搜索结果和回跳恢复状态可同时感知。
- 2026-03-17 user 闭环 154：用户页返回单位管理路由支持回带单位搜索关键词，跨模块回跳后单位页筛选视角可恢复。
- 2026-03-17 unit 闭环 155：单位页跳转用户页时补充 unitKeyword，上下文回跳不再只恢复单位ID，也能恢复单位搜索条件。
- 2026-03-17 unit 闭环 156：单位页支持从路由读取并恢复 keyword，单位搜索条件在用户页往返后真正可还原。
- 2026-03-17 user 闭环 157：用户页返回单位管理时补充回传用户筛选与目标用户ID，为二次回跳恢复原用户工作面打底。
- 2026-03-17 unit 闭环 158：单位页返回用户管理入口支持恢复用户关键词、状态与目标详情，跨模块往返后的用户工作面可继续衔接。
- 2026-03-17 unit 闭环 159：单位页来源提示补充“可恢复用户页上下文”反馈，用户页与单位页来回后的状态预期更明确。
- 2026-03-17 unit 闭环 160：单位详情区补充返回用户管理入口，详情内就能直接回到原用户工作面继续查看。
- 2026-03-17 user 闭环 161：用户页来源提示补充“已保留返回用户工作面上下文”反馈，双向回跳链路在两端都可感知。
- 2026-03-17 user 闭环 162：用户页来源提示补充来源单位搜索关键词，跨模块落点不再只知道来自哪个单位，也知道来自哪个单位筛选视角。
- 2026-03-17 user 闭环 163：用户详情摘要补充来源单位搜索关键词，进入详情后仍可直接确认来源单位的筛选视角。
- 2026-03-17 unit 闭环 164：单位详情补充“返回用户管理将恢复哪些条件”摘要，双向回跳后的用户工作面恢复范围更明确。
- 2026-03-17 unit 闭环 165：单位页搜索空态补充返回用户管理入口，即使单位筛空也能直接回到原用户工作面。
- 2026-03-17 unit 闭环 166：单位详情补充当前单位搜索词展示，回到单位页后的当前筛选视角在详情内也可直接确认。
- 2026-03-17 user 闭环 167：用户页焦点提示补充来源单位搜索词，详情打开态下也能直接确认来源单位筛选视角。
- 2026-03-17 user 闭环 168：用户页空态补充来源单位搜索词，筛选为空时仍能判断当前来自哪个单位筛选视角。
- 2026-03-17 unit 闭环 169：单位页顶部来源提示补充“返回用户页将恢复哪些条件”说明，不进入详情也能明确回跳恢复范围。
- 2026-03-17 unit 闭环 170：单位页筛选摘要补充“返回用户页将恢复哪些条件”说明，有结果时也能直接确认双向回跳恢复范围。
- 2026-03-17 unit 闭环 171：单位页搜索空态补充目标用户ID提示，返回用户页前即可明确会继续定位到哪个用户详情。
- 2026-03-17 unit 闭环 172：单位详情补充返回用户关键词展示，用户工作面恢复内容从摘要拆到字段级可见。
- 2026-03-17 unit 闭环 173：单位详情补充返回用户状态展示，回跳后会恢复哪类用户状态筛选可直接确认。
- 2026-03-17 unit 闭环 174：单位详情补充返回用户定位展示，回跳后会继续定位哪个用户详情在详情区可直接确认。
- 2026-03-17 unit 闭环 175：单位页顶部来源提示补充返回用户关键词展示，不进入详情也能看到将恢复的用户关键词。
- 2026-03-17 unit 闭环 176：单位页顶部来源提示补充返回用户状态展示，不进入详情也能看到将恢复的用户状态。
- 2026-03-17 unit 闭环 177：单位页顶部来源提示补充返回用户定位展示，不进入详情也能看到将恢复的目标用户ID。
- 2026-03-17 unit 闭环 178：单位页筛选摘要补充返回用户定位展示，有结果时也能直接确认会回到哪个用户详情。
- 2026-03-17 unit 闭环 179：单位页搜索空态补充返回用户关键词展示，即使单位筛空也能直接看到将恢复的用户关键词。
- 2026-03-17 unit 闭环 180：单位页搜索空态补充返回用户状态展示，即使单位筛空也能直接看到将恢复的用户状态。
- 2026-03-17 unit 闭环 181：单位页搜索空态补充返回用户恢复摘要，即使单位筛空也能一次性看到完整回跳上下文。
- 2026-03-17 unit 闭环 182：单位页筛选摘要补充返回用户关键词展示，有结果时也能直接看到将恢复的用户关键词。
- 2026-03-17 unit 闭环 183：单位页筛选摘要补充返回用户状态展示，有结果时也能直接看到将恢复的用户状态。
- 2026-03-17 unit 闭环 184：单位页补充来源单位搜索词计算字段，为来源单位筛选视角做字段级展示准备。
- 2026-03-17 unit 闭环 185：单位页顶部来源提示补充来源单位搜索词展示，不进入详情也能直接确认当前恢复到哪个单位筛选视角。
- 2026-03-17 unit 闭环 186：单位页筛选摘要补充来源单位搜索词展示，有结果时也能直接确认当前单位筛选视角。
- 2026-03-17 unit 闭环 187：单位页搜索空态补充来源单位搜索词展示，即使单位筛空也能确认当前来源单位筛选视角。
- 2026-03-17 unit 闭环 188：单位详情补充来源单位搜索词展示，进入详情后也能继续确认来源单位筛选视角。
- 2026-03-17 unit 闭环 189：单位页补充来源单位视角摘要字段，把来源单位和来源搜索词收口成统一展示文案。
- 2026-03-17 unit 闭环 190：单位页顶部来源提示补充来源单位视角摘要，不进入详情也能直接确认恢复到哪个单位筛选视角。
- 2026-03-17 unit 闭环 191：单位页筛选摘要补充来源单位视角摘要，有结果时也能一次性确认当前来源单位筛选视角。
- 2026-03-17 unit 闭环 192：单位页搜索空态补充来源单位视角摘要，即使单位筛空也能一次性确认当前来源单位筛选视角。
- 2026-03-17 unit 闭环 193：单位详情补充来源单位视角摘要，进入详情后也能一次性确认当前来源单位筛选视角。
- 2026-03-17 unit 闭环 194：单位卡片补充来源单位标识，列表态下也能直接定位当前从用户页恢复回来的目标单位。
- 2026-03-17 unit 闭环 195：来源单位卡片补充来源单位视角摘要，列表态下也能直接看到恢复回来的单位筛选视角。
- 2026-03-17 unit 闭环 196：单位详情补充“来源单位与当前查看单位”对照提示，切换到其他单位后也不会丢掉来源上下文。
- 2026-03-17 unit 闭环 197：成员区摘要补充来源单位视角，进入成员浏览阶段后仍能确认当前来源上下文。
- 2026-03-17 unit 闭环 198：成员搜索空态补充来源单位视角与返回用户入口，成员筛空时仍能继续沿原工作面回跳。
- 2026-03-17 unit 闭环 199：来源单位卡片补充返回用户恢复摘要，列表态下也能直接看到回跳后会恢复哪些用户条件。
- 2026-03-17 unit 闭环 200：成员区摘要补充返回用户恢复摘要，进入成员浏览阶段后仍能确认回跳会恢复哪些用户条件。
- 2026-03-17 unit 闭环 201：成员搜索空态补充返回用户恢复摘要，成员筛空时也能继续确认回跳会恢复哪些用户条件。
- 2026-03-17 unit 闭环 202：单位详情补充“切换到其他单位后仍定位目标用户”提示，避免误解当前查看单位会改写回跳定位。
- 2026-03-17 unit 闭环 203：负责人候选操作区补充“返回用户管理原工作面”入口，详情操作区内可直接回到原用户工作面。
- 2026-03-17 unit 闭环 204：成员卡片补充来源单位视角展示，浏览单个成员时也能直接确认当前来源上下文。
- 2026-03-17 unit 闭环 205：成员卡片补充返回用户恢复摘要，浏览单个成员时也能直接看到回跳会恢复哪些用户条件。
- 2026-03-17 unit 闭环 206：成员卡片补充返回定位用户ID展示，浏览单个成员时也能直接看到回跳会定位到哪个用户。
- 2026-03-17 unit 闭环 207：目标成员卡片补充“就是回跳定位用户”提示，成员列表里可直接识别回到用户页后会打开哪位用户。
- 2026-03-17 unit 闭环 208：目标成员跳转入口补充“当前回跳目标”文案，点击前就能知道该入口是否与原用户工作面对齐。
- 2026-03-17 unit 闭环 209：负责人候选补充“就是回跳定位用户”提示，负责人候选区也能直接识别是否与原用户工作面对齐。
- 2026-03-17 unit 闭环 210：负责人候选跳转入口补充“当前回跳目标”文案，点击前即可确认是否与原用户工作面对齐。
- 2026-03-17 unit 闭环 211：成员区摘要补充返回定位用户ID提示，成员浏览阶段也能直接确认回到用户页后会定位哪位用户。
- 2026-03-17 unit 闭环 212：成员空态补充返回定位用户ID提示，成员筛空时也能直接确认回到用户页后会定位哪位用户。
- 2026-03-17 unit 闭环 213：成员区操作区补充“返回用户管理原工作面”入口，成员浏览阶段也能直接回到原用户工作面。
- 2026-03-17 unit 闭环 214：成员区操作区回跳入口去重并统一到“返回用户管理原工作面”，成员浏览阶段的回跳意图更明确。
- 2026-03-17 unit 闭环 215：负责人候选摘要补充返回用户恢复内容，切到负责人候选区时也能直接确认回跳会恢复哪些用户条件。
- 2026-03-17 unit 闭环 216：负责人候选摘要补充返回定位用户ID提示，切到负责人候选区时也能直接确认回跳会定位哪位用户。
- 2026-03-17 unit 闭环 217：成员空态操作区补充“返回用户管理原工作面”入口，成员筛空时也能直接回到原用户工作面。
- 2026-03-17 unit 闭环 218：成员卡片补充“返回用户管理原工作面”入口，浏览单个成员时也能就地回到原用户工作面。
- 2026-03-17 unit 闭环 219：成员空态回跳入口去重并统一到“返回用户管理原工作面”，成员筛空时的回跳意图更明确。
- 2026-03-17 unit 闭环 220：负责人候选补充“返回原工作面仍会打开当前候选”提示，负责人候选区对回跳结果反馈更直接。
- 2026-03-17 unit 闭环 221：目标成员卡片补充“返回原工作面仍会打开当前成员”提示，成员列表对回跳结果反馈更直接。
- 2026-03-17 unit 闭环 222：目标成员卡片的回跳入口补充“仍会打开当前成员”文案，点击前即可确认回跳结果与当前卡片一致。
- 2026-03-17 unit 闭环 223：成员空态补充“搜索为空也仍会定位目标用户”提示，成员筛空时的回跳结果更明确。
- 2026-03-17 unit 闭环 224：负责人候选补充“不是原回跳目标”提示，切到其他候选时也能明确原用户工作面不会被改写。
- 2026-03-17 unit 闭环 225：非目标成员卡片补充“不是原回跳目标”提示，浏览其他成员时也能明确原用户工作面不会被改写。
- 2026-03-17 unit 闭环 226：非目标成员卡片补充“返回原工作面仍回到原目标”提示，成员列表里可直接确认回跳不会落到当前成员。
- 2026-03-17 unit 闭环 227：非目标负责人候选入口补充“返回原工作面仍回到原目标”提示，负责人候选区可直接确认回跳不会落到当前候选。
- 2026-03-17 unit 闭环 228：成员区补充“当前列表未命中原回跳目标”提示，成员筛选后也能明确回跳仍会回到原用户目标。
- 2026-03-17 unit 闭环 229：成员区补充“当前列表已命中原回跳目标”提示，命中原用户目标时可直接确认不必回跳也能继续查看。
- 2026-03-17 unit 闭环 230：成员空态补充“搜索未命中不会改写原回跳目标”提示，成员筛空时对回跳稳定性的反馈更完整。
- 2026-03-17 unit 闭环 231：负责人候选入口补充“与原工作面完全对齐”提示，命中原目标时可直接确认无需回跳也能继续查看。
- 2026-03-17 unit 闭环 232：目标成员入口补充“与原工作面完全对齐”提示，命中原目标时可直接确认无需回跳也能继续查看。
- 2026-03-17 unit 闭环 233：成员区补充“当前搜索仍保留原回跳目标”提示，成员筛选命中目标时反馈更完整。
- 2026-03-17 unit 闭环 234：成员区补充“当前搜索已偏离原回跳目标”提示，成员筛选未命中目标时反馈更完整。
- 2026-03-17 unit 闭环 235：目标成员卡片补充“当前搜索仍命中原回跳目标”提示，命中目标时成员卡片反馈更完整。
- 2026-03-17 unit 闭环 236：非目标成员卡片补充“当前搜索已偏离原回跳目标”提示，未命中目标时成员卡片反馈更完整。
- 2026-03-17 unit 闭环 237：负责人候选补充“成员搜索态下仍与原回跳目标对齐”提示，跨区浏览时的回跳反馈更完整。
- 2026-03-17 unit 闭环 238：负责人候选补充“成员搜索态下也未命中原回跳目标”提示，跨区浏览时的回跳反馈更完整。
- 2026-03-17 user 闭环 239：用户管理页完成“用户列表展示”功能闭环，补充当前页展示区间、字段完成度摘要和列表卡片状态标签，列表可读性显著提升。
- 2026-03-17 user 闭环 240：用户列表摘要补充本页状态结构展示，列表顶部可直接判断当前页启停分布。
- 2026-03-17 user 闭环 241：用户列表摘要补充本页资料完整人数，列表顶部可直接判断当前页资料补齐程度。
- 2026-03-17 user 闭环 242：用户列表摘要补充目标用户是否在当前页，列表顶部可直接判断是否需要翻页继续找目标用户。
- 2026-03-17 user 闭环 243：用户卡片补充资料完整/待补资料标签，列表扫读时可直接判断资料完整度。
- 2026-03-17 user 闭环 244：用户卡片补充本页定位与资料完成度展示，列表逐卡浏览时的位置信息和资料状态更完整。
- 2026-03-17 user 闭环 245：用户列表摘要补充本页待补资料人数，列表顶部可直接判断当前页还剩多少资料未补齐用户。
- 2026-03-17 user 闭环 246：用户列表摘要补充本页本人数量，列表顶部可直接判断当前页是否包含当前登录用户。
- 2026-03-17 user 闭环 247：用户卡片补充资料缺口展示，列表逐卡浏览时可直接判断缺的是岗位还是手机号。
- 2026-03-17 user 闭环 248：用户卡片补充更新时间状态展示，列表逐卡浏览时可直接判断数据是否近期更新。
- 2026-03-17 user 闭环 249：用户列表摘要补充当前页命中的目标用户标签，列表顶部可直接确认目标用户姓名与账号。
- 2026-03-17 user 闭环 250：用户列表摘要补充本页近期更新人数，列表顶部可直接判断当前页近期更新覆盖情况。
- 2026-03-17 user 闭环 251：用户卡片补充“近期更新/待核对更新”标签，列表扫读时可直接判断更新时效。
- 2026-03-17 user 闭环 252：用户卡片补充账号状态摘要，列表逐卡浏览时可直接判断启停与挂接状态组合。
- 2026-03-17 user 闭环 253：目标用户卡片补充来源链路命中提示，列表逐卡浏览时可直接确认哪个卡片对应当前目标用户。
- 2026-03-17 user 闭环 254：本人用户卡片补充本页定位提示，列表逐卡浏览时可直接确认当前登录账号位于本页第几位。
- 2026-03-17 home 闭环 255：首页骨架统一为“查询区 + 内容区 + 操作区 + 空状态”结构，首页可按统一骨架标准访问和查看。
- 2026-03-17 user 闭环 256：用户管理页补充骨架接入说明，确认该页已具备查询区、操作区、内容区和空状态骨架。
- 2026-03-17 orgtree 闭环 257：组织管理页补充查询区骨架说明，在保留现有树能力的同时对齐主框架骨架标准。
- 2026-03-17 weeklywork 闭环 258：周报管理页统一为骨架页结构，补齐查询区、内容区、操作区和空状态。
- 2026-03-17 weeklywork 闭环 270：周报管理页接入真实列表数据，支持按周次与状态加载周报卡片列表。
- 2026-03-17 weeklywork 闭环 271：周报管理页补齐提交链路，支持从当前编辑区或列表草稿直接提交周报。
- 2026-03-17 weeklywork 闭环 272：周报管理页补齐编辑链路，支持本人草稿或退回周报回填后继续暂存修改。
- 2026-03-17 weeklywork 闭环 273：周报管理页补齐详情查看链路，支持点击列表打开周报详情弹层查看完整内容。
- 2026-03-17 weeklywork 闭环 274：周报管理页补齐审核链路，支持对待审核周报执行通过或退回并立即看到状态结果。
- 2026-03-17 orgtree 闭环 275：组织管理页接入真实组织树展示与节点详情联动，节点切换后可同步查看直属下级和上级链。
- 2026-03-17 orgtree 闭环 276：组织管理页补齐新增组织节点链路，支持在当前节点下直接新增下级并刷新树结果。
- 2026-03-17 orgtree 闭环 277：组织管理页补齐编辑组织节点链路，支持直接修改节点姓名、岗位和手机号。
- 2026-03-17 orgtree 闭环 278：组织管理页补齐删除组织节点链路，支持删除叶子节点并立即回看树结果。
- 2026-03-17 orgtree 闭环 279：组织管理页补齐节点启停链路，支持在详情区直接启用或停用当前节点。
- 2026-03-17 param 闭环 280：参数配置页接入真实参数列表，支持查看参数编码、名称、值、备注和状态。
- 2026-03-17 param 闭环 281：参数配置页补齐参数查询，支持按编码/名称关键词和状态筛选列表结果。
- 2026-03-17 param 闭环 282：参数配置页补齐参数新增，支持直接录入参数编码、名称、值和状态后保存。
- 2026-03-17 param 闭环 283：参数配置页补齐参数编辑，支持回填现有参数并修改名称、值、备注和状态。
- 2026-03-17 param 闭环 284：参数配置页补齐参数删除与启停，支持直接删除参数或切换启停状态。
- 2026-03-17 attendance 闭环 285：考勤管理页接入真实考勤列表，支持查看打卡人、打卡类型、时间、地点和有效状态。
- 2026-03-17 attendance 闭环 286：考勤管理页补齐考勤查询，支持按用户、打卡类型和时间范围筛选记录。
- 2026-03-17 attendance 闭环 287：考勤管理页补齐上班/下班打卡链路，支持直接点击提交当天打卡并刷新结果。
- 2026-03-17 attendance 闭环 288：考勤管理页补齐编辑与补录链路，支持新增补录记录或回填已有记录继续修改。
- 2026-03-17 attendance 闭环 289：考勤管理页补齐删除与状态处理，支持删除记录或切换有效/无效状态。
- 2026-03-17 attendance 闭环 290：考勤管理页切换到单日单条记录模型，列表按姓名、组织、考勤日期和上下班时间展示真实结果。
- 2026-03-17 attendance 闭环 291：考勤管理页补齐姓名/组织/日期范围查询，查询结果可直接筛出目标考勤记录。
- 2026-03-17 attendance 闭环 292：考勤管理页补齐最小打卡链路，当天无记录时新增上班记录，有记录时自动补下班时间。
- 2026-03-17 attendance 闭环 293：考勤管理页补齐统一 save 的编辑/补录链路，支持按用户和日期新增或回填修改考勤。
- 2026-03-17 attendance 闭环 294：考勤管理页补齐删除链路，可直接删除指定日期的考勤记录并刷新列表结果。
- 2026-03-17 workscore 闭环 295：工作评分页接入按周评分列表，支持查看姓名、组织、考勤得分、周报得分、纪律得分和总分。
- 2026-03-17 workscore 闭环 296：工作评分页补齐按周次和组织查询，查询后可直接筛出目标周评分结果。
- 2026-03-17 workscore 闭环 297：工作评分页补齐手动 calculate 链路，可基于考勤和周报按周生成或覆盖评分结果。
- 2026-03-17 workscore 闭环 298：工作评分页补齐评分详情查看，支持打开详情查看评分构成和纪律说明。
- 2026-03-17 workscore 闭环 299：工作评分页补齐按总分排序，列表可直接查看当前周评分排名。
- 2026-03-17 workscore 闭环 300：工作评分页升级排行榜视图，评分结果增加排名字段并按总分倒序展示。
- 2026-03-17 workscore 闭环 301：工作评分页补齐 NORMAL / YELLOW / RED 状态标识，评分计算后会按总分阈值回写状态。
- 2026-03-17 workscore 闭环 302：工作评分页补齐红牌名单筛选，可一键筛出 RED 状态人员。
- 2026-03-17 workscore 闭环 303：工作评分页补齐 Top10 优秀榜，可直接查看当前周前十名人员。
- 2026-03-17 workscore 闭环 304：工作评分页补齐组织平均分统计，可直接查看各组织平均分和人数。
- 2026-03-17 workscore 闭环 305：评分系统补齐 Word 导出接口 /score/report/export，当前周评分结果可直接导出通报文档。
- 2026-03-17 workscore 闭环 306：导出文档补齐标题与时间范围，通报首页可直接展示周次和起止日期。
- 2026-03-17 workscore 闭环 307：导出文档补齐总体情况，支持输出人数与平均分概览。
- 2026-03-17 workscore 闭环 308：导出文档补齐 Top10 优秀人员表，便于领导直接查看优秀名单。
- 2026-03-17 workscore 闭环 309：导出文档补齐红牌人员表，便于直接查看低分与红牌人员。
- 2026-03-17 workscore 闭环 310：导出文档补齐组织平均分排名表，便于查看组织整体表现排序。
- 2026-03-17 operationlog 闭环 311：新增 operation_log 表，支持按模块、操作人、业务ID和时间记录关键操作日志。
- 2026-03-17 operationlog 闭环 312：在用户新增、用户编辑、周报审核、考勤补录和评分计算中补齐显式日志写入，关键操作已可追溯。
- 2026-03-17 operationlog 闭环 313：操作日志页接入真实列表展示，可直接查看模块、操作人、时间和操作内容。
- 2026-03-17 operationlog 闭环 314：操作日志页补齐模块、操作人和时间范围查询，可直接筛出目标日志记录。
- 2026-03-17 statistics 闭环 315：统计分析页补齐总体概览，支持查看平均分、红牌人数、优秀人数和评分覆盖率。
- 2026-03-17 statistics 闭环 316：统计分析页补齐组织排名，支持按组织查看平均分、红牌人数和覆盖人数。
- 2026-03-17 statistics 闭环 317：统计分析页补齐红牌名单，可直接筛出当前周 RED 状态人员。
- 2026-03-17 statistics 闭环 318：统计分析页补齐黄牌名单，可直接筛出当前周 YELLOW 状态人员。
- 2026-03-17 statistics 闭环 319：统计分析页补齐最近 4 周趋势，支持查看各周平均分、红黄牌人数和覆盖人数。
- 2026-03-17 rbac 闭环 320：用户表新增 role 字段并接入 ADMIN / USER 最小角色模型，默认角色为 USER。
- 2026-03-17 rbac 闭环 321：登录结果补齐 role 字段并写入前端登录态，JWT 与前端缓存均已保留角色信息。
- 2026-03-17 rbac 闭环 322：后端关键权限收口完成，ADMIN 才能执行评分计算、通报导出和用户管理，USER 查询范围收口为仅本人数据。
- 2026-03-17 rbac 闭环 323：前端菜单、路由与关键操作按钮完成 ADMIN 可见控制，普通用户不再看到用户管理入口和评分关键操作按钮。
- 2026-03-17 delivery 闭环 324：单位管理补齐交付基础，新增 sys_org_unit 兼容表、初始化脚本，并将单位列表按当前用户 unit_id 做最小隔离。
- 2026-03-17 delivery 闭环 325：权限增强补齐菜单级控制与核心接口 ADMIN 校验，单位、参数、组织架构、日志查询等管理入口已收口到管理员。
- 2026-03-17 delivery 闭环 326：操作日志补齐登录、删除、通报导出和权限拒绝记录，关键交付动作具备基础追溯能力。
- 2026-03-17 delivery 闭环 327：规则微增强落地，考勤补录新增时间合法性校验，周报未交时评分总分封顶 59 分，统计覆盖率改按启用用户计算。
- 2026-03-17 delivery 闭环 328：交付能力补齐 README、初始化 SQL、演示路径与环境配置说明，项目已具备最小交付说明材料。
- 2026-03-17 delivery 闭环 329：单位管理补齐最小数据库脚本与后端 CRUD，sys_org_unit 已支持列表、新增、编辑、删除和启停，并同步兼容 sys_unit。
- 2026-03-17 delivery 闭环 330：单位管理页切换为真实维护页，前端已可直接新增、编辑、删除和启停单位。
- 2026-03-17 delivery 闭环 331：统一数据范围增强为“超管全量、普通用户按 unit_id 过滤”，考勤、周报、评分、统计与组织查询已复用统一 unit scope。
- 2026-03-17 delivery 闭环 332：组织架构页补齐所属单位字段，新增和编辑组织节点时已可选择 unit_id。
- 2026-03-17 delivery 闭环 333：用户管理页补齐单位名称只读展示，列表和详情均可直接查看所属单位信息。
- 2026-03-17 delivery 闭环 334：新增 DataScopeContext、CurrentUserFacade、DataScopeHelper 与 UnitScopedRequest，单位过滤已有统一可复用结构。
- 2026-03-17 delivery 闭环 335：用户、考勤、周报、评分、日志查询 DTO 已统一支持 unitId 注入，后续模块可直接复用同一模式。
- 2026-03-17 delivery 闭环 336：考勤、周报、评分、日志查询 SQL 已改为优先按 unit_id 过滤，superAdmin 与普通用户过滤逻辑不再分散写。
- 2026-03-17 delivery 闭环 337：统计分析查询统一切到 unit_id 数据范围，组织排名、红黄牌、趋势与覆盖率口径已与单位过滤对齐。
- 2026-03-17 delivery 闭环 338：用户查询 SQL 已补 unit_id 条件并统一使用 sys_org_unit 关联单位信息，单位管理与查询结构进一步标准化。
- 2026-03-17 delivery 闭环 339：单位删除前新增关联组织显式校验，sys_org_node 仍挂在该单位时会直接阻止删除，单位管理维护规则更完整。
- 2026-03-17 delivery 闭环 340：operation_log 正式补齐 unit_id 字段、历史回填与索引，日志数据开始直接落单位维度。
- 2026-03-17 delivery 闭环 341：日志写入改为同步记录当前用户 unit_id，避免后续仅靠 operator_id 反查导致单位归属漂移。
- 2026-03-17 delivery 闭环 342：日志查询切换为优先按 operation_log.unit_id 过滤，并保留旧数据按 operator_id 回退兼容。
- 2026-03-17 orgtree 闭环 343：修复新增下级用户未设置默认 role 导致的保存失败，组织树创建下级用户已与 RBAC Lite 字段约束重新对齐。
- 2026-03-17 attendance 闭环 259：考勤管理页统一为骨架页结构，补齐查询区、内容区、操作区和空状态。
- 2026-03-17 statistics 闭环 260：统计分析页统一为骨架页结构，补齐查询区、内容区、操作区和空状态。
- 2026-03-17 param 闭环 261：参数配置页统一为骨架页结构，补齐查询区、表格区、操作区和空状态。
- 2026-03-17 operationlog 闭环 262：操作日志页统一为骨架页结构，补齐查询区、表格区、操作区和空状态。
- 2026-03-17 profile 闭环 263：个人中心页完成骨架接入并加入菜单与路由，页面可访问、可进入、可查看。
- 2026-03-17 permission 闭环 264：无权限页与 404 页完成骨架标准化，异常页已具备内容区、操作入口和空状态反馈。
- 2026-03-17 user 闭环 265：用户列表真实数据联调补齐关键词去空格与按更新时间倒序展示，列表数据顺序和查询结果与真实维护节奏对齐。
- 2026-03-17 user 闭环 266：用户查询功能补齐路由同步，查询与重置后刷新页面仍可保留当前筛选条件。
- 2026-03-17 user 闭环 267：用户新增功能补齐“清空筛选并回到第一页后直达新用户详情”，新增完成后可直接验收结果。
- 2026-03-17 user 闭环 268：用户编辑功能补齐编辑态只读身份信息，编辑时可直接确认账号与单位上下文。
- 2026-03-17 user 闭环 269：用户状态切换功能接入列表操作区，用户管理页已可直接执行启用/停用闭环。
- 2026-03-18 home 闭环 344：首页补齐登录摘要，进入系统后可直接查看当前账号、角色、用户ID与可见菜单范围。
- 2026-03-18 home 闭环 345：首页补齐角色分流快捷入口，管理员与普通用户进入系统后都可直接跳转到各自常用工作面。
- 2026-03-18 home 闭环 346：首页补齐当前账号可访问模块清单，切换角色后无需再逐个点菜单确认访问范围。
- 2026-03-18 profile 闭环 347：个人中心补齐当前账号资料卡片，页面已可直接查看姓名、账号、用户ID与角色信息。
- 2026-03-18 profile 闭环 348：个人中心补齐账号状态说明，当前登录状态、菜单范围与默认工作面都可直接查看。
- 2026-03-18 orgtree 闭环 349：修复组织树查询误按 unit_id 过滤导致新增下级已入库却不显示的问题，组织架构页现按真实父子链与 tree_path 回显节点。
- 2026-03-18 orgtree 闭环 350：组织树新增下级时补齐后端单位一致性校验，下级节点必须继承上级节点所属单位，避免再写出跨单位脏结构。
- 2026-03-18 orgtree 闭环 351：组织树新增弹层收口单位入口，前端改为只读展示“自动继承上级单位”，减少误操作预期。
- 2026-03-18 orgtree 闭环 352：组织树编辑节点时补齐后端单位锁定校验，当前页面不再允许直接修改节点所属单位，避免树结构与单位归属分叉。
- 2026-03-18 orgtree 闭环 353：组织树编辑弹层同步收口单位入口，前端改为只读提示“当前页面不支持改单位”，前后端规则重新对齐。
- 2026-03-18 orgtree 闭环 354：组织树详情区补齐单位继承规则提示，浏览节点时即可确认当前页面仅维护树结构与节点资料，不直接改单位归属。
- 2026-03-18 orgtree 闭环 355：组织树服务层补齐单位继承与单位锁定单测，新增下级和编辑节点的单位约束已具备回归保护。
- 2026-03-18 user 闭环 356：用户管理页补齐统一回刷反馈区，新增、编辑、删除与重置密码后的真实回刷结果已有稳定展示位。
- 2026-03-18 user 闭环 357：用户新增成功后补齐页面内真实回刷反馈，已明确提示“清空筛选、回到第一页并打开新用户详情”的结果。
- 2026-03-18 user 闭环 358：用户编辑成功后补齐页面内真实回刷反馈，已明确提示“列表重刷并同步详情”的结果。
- 2026-03-18 user 闭环 359：用户删除与重置密码操作补齐真实回刷反馈，重置密码成功后已自动刷新列表与当前详情状态。
- 2026-03-18 user 闭环 360：用户列表与详情的数据权限联调切到 tree_path 子树范围，普通用户现可查看自己及下级，并补齐服务层回归测试。
- 2026-03-18 attendance 闭环 361：考勤查询接口切到 currentUser.tree_path 子树过滤，普通用户现仅能查询自己及下级的考勤记录。
- 2026-03-18 attendance 闭环 362：考勤补录与删除的可管理性校验切到 tree_path 子树判断，当前用户只能维护自己及下级的考勤数据。
- 2026-03-18 weeklywork 闭环 363：周报查询接口切到 currentUser.tree_path 子树过滤，普通用户现仅能查询自己及下级的周报记录。
- 2026-03-18 weeklywork 闭环 364：周报详情与审核可读性校验切到 tree_path 子树判断，普通用户仅能查看自己及下级的周报。
- 2026-03-18 permission 闭环 365：新增 DataScopeService 与 TreePathScopedRequest，tree_path 前缀构建与树路径可读判断已有统一入口。
- 2026-03-18 attendance 闭环 366：attendance 模块删除 Service 内手写 tree_path 判断，查询与可管理校验已统一改走 DataScopeService。
- 2026-03-18 weeklywork 闭环 367：weeklywork 模块删除 Service 内手写 tree_path 判断，查询、详情与审核可读校验已统一改走 DataScopeService。
- 2026-03-18 permission 闭环 368：attendance 与 weeklywork 的 Mapper tree_path 条件写法统一为“treePathPrefix 非空即追加 LIKE 前缀过滤”，相关测试已全部通过。
- 2026-03-18 statistics 闭环 369：StatisticsServiceImpl 与 StatisticsMapper 全量切到 treePathPrefix 范围过滤，统计查询已移除 unit_id 数据权限依赖。
- 2026-03-18 statistics 闭环 370：首页概览联动的考勤人数与周报提交人数统计同步切到 tree_path 子树范围，统计口径与 DataScopeService 保持一致。
- 2026-03-18 workscore 闭环 371：workscore 查询、详情、批量计算与导出统一接入 DataScopeService，评分导出和批量评分现仅覆盖当前用户子树范围。
- 2026-03-18 operationlog 闭环 372：operationlog 查询切到 treePathPrefix 过滤，并完成 Mapper 全局扫描确认剩余未接入项仅为系统配置/结构类查询，不属于业务数据权限覆盖对象。
- 2026-03-18 permission 闭环 373：删除 attendance 与 weeklywork 中遗留的 DataScopeHelper 注入与空壳调用点，业务查询与校验现仅保留 DataScopeService 入口。
- 2026-03-18 permission 闭环 374：清理 attendance、weeklywork、workscore、operationlog 的 QueryRequest 中 unitId 权限字段，查询请求只保留 treePathPrefix 范围字段。
- 2026-03-18 permission 闭环 375：清理 attendance、weeklywork、workscore Mapper 中 request.unitId 权限过滤分支，保留 unitName 等业务筛选字段。
- 2026-03-18 permission 闭环 376：统一 attendance 与 weeklywork 服务层可读/可操作校验为 DataScopeService.validateReadableUser，permissionService.queryDataScopeUserIds 也已切到 tree_path 子树口径。
- 2026-03-18 permission 闭环 377：DataScopeService 增加非管理员 treePath 必填防御校验，并删除已零引用的 DataScopeHelper 旧实现，相关回归测试全部通过。
- 2026-03-18 permission 闭环 378：新增 DataScopeType（SELF / SUB_TREE / CUSTOM），DataScopeService 已具备权限类型解析与范围描述能力，为后续扩展预留统一入口。
- 2026-03-18 permission 闭环 379：DataScopeService 新增 tree_path -> userIds 读缓存，PermissionServiceImpl 查询数据范围用户集已复用缓存结果，减少重复子树解析成本。
- 2026-03-18 permission 闭环 380：用户详情查询补齐数据访问日志，现可记录“谁查看了谁的数据”，便于后续审计查看链路。
- 2026-03-18 user 闭环 381：用户分页接口新增权限范围描述、范围人数与范围类型字段，用户管理页现可直接展示“当前可查看多少人”的权限摘要。
- 2026-03-18 user 闭环 382：UserQueryRequest.unitId 补齐“仅业务筛选、非权限字段”的注释与保护逻辑，避免后续误把 unitId 当作数据权限入口；后端 25 项回归测试全部通过，前端构建仍受本地缺少 @rollup/rollup-linux-x64-gnu 阻塞。
- 2026-03-18 unit 闭环 383：新增 attendance_location 建表 SQL 文件，先按“一个单位一个主打卡点”落表，字段已覆盖名称、经纬度、半径、地址、状态与时间戳。
- 2026-03-18 unit 闭环 384：新增“查询某单位打卡点”接口，单位管理现可按 unitId 读取该单位主打卡点信息并回填弹窗表单。
- 2026-03-18 unit 闭环 385：新增“保存/更新某单位打卡点”接口，后端已按 unitId 做单记录 upsert，表中有记录则更新，无记录则新增。
- 2026-03-18 unit 闭环 386：单位管理列表新增“打卡点设置”按钮与弹窗表单，现可维护打卡点名称、半径、经纬度、地址与启用状态。
- 2026-03-18 unit 闭环 387：单位管理弹窗补齐地图选点最小交互，支持浏览器定位、点击选点、经纬度与地址自动回填；后端 UnitServiceImplTest 5 项通过，前端构建仍受本地缺少 @rollup/rollup-linux-x64-gnu 阻塞。
- 2026-03-18 frontend 闭环 388：补装本地缺失的 @rollup/rollup-linux-x64-gnu 依赖并更新 package-lock，npm run build 已恢复通过，Vite/Rollup 本地构建阻塞已解除。
- 2026-03-18 unit 闭环 389：新增 amap SDK 接入层，单位打卡点弹窗现可在配置 VITE_AMAP_KEY 时切换到正式地图模式，未配置时仍保留轻量选点回退。
- 2026-03-18 unit 闭环 390：单位列表补齐“是否已配置打卡点”与打卡点摘要展示，列表现可直接确认各单位主打卡点名称、状态、半径与地址摘要。
- 2026-03-18 unit 闭环 391：打卡点弹窗补齐正式地图标记与地址回填优化，支持 SDK 地图点击选点、逆地理解析、地图定位优先与浏览器定位回退。
- 2026-03-18 unit 闭环 392：打卡点保存后补齐页面内反馈与单位列表回刷，保存成功后会提示当前单位已完成打卡点更新并刷新列表状态；UnitServiceImplTest 6 项通过，npm run build 通过。
- 2026-03-18 unit 闭环 393：补齐地图 Key 配置说明，新增前端 .env 示例并在 README 说明 VITE_AMAP_KEY 配置方式；未配置 Key 时会自动回退到轻量选点模式。
- 2026-03-18 unit 闭环 394：新增单位打卡点只读详情卡片，单位列表现可直接查看打卡点名称、地址、经纬度、半径与状态，不进入编辑态也能验收配置结果。
- 2026-03-18 unit 闭环 395：优化打卡点保存后的详情同步，保存后除列表回刷外还会重新拉取当前单位打卡点详情，弹窗内只读卡片与表单会同步显示最新结果，并给出明确页面反馈。
- 2026-03-18 unit 闭环 396：补齐无地图 Key 与地图加载失败时的页面提示，弹窗现会明确展示“轻量选点模式”说明，用户可直接继续选点与保存，不会因 SDK 不可用而阻塞。
- 2026-03-18 unit 闭环 397：补齐单位打卡点最小联调文档，已记录查询接口、保存接口、前端地图 Key 配置方式，以及正式地图模式与轻量模式的差异。
- 2026-03-18 attendance 闭环 398：新增“按当前登录用户查所属单位主打卡点”接口，当前用户通过 sys_user.unit_id 关联单位并可读取 attendance_location；若未绑定单位、未配置或未启用，会返回明确状态说明。
- 2026-03-18 attendance 闭环 399：考勤 check-in 接口补齐打卡距离校验，现支持输入当前经纬度、计算与单位主打卡点距离，并返回距离、是否允许打卡与失败原因。
- 2026-03-18 attendance 闭环 400：员工打卡页接入浏览器定位，点击“立即打卡”时会获取当前位置并把经纬度传给后端；定位失败会给出明确提示。
- 2026-03-18 attendance 闭环 401：员工打卡页补齐结果回显，现可直接看到打卡成功/失败、距离、是否允许打卡，以及未配置打卡点、超出范围、定位失败等原因。
- 2026-03-18 attendance 闭环 402：补齐打卡最小落库字段 SQL 与后端持久化逻辑，成功打卡后会记录经纬度、距离与结果；当前以 attendance_record 扩展字段承载，AttendanceServiceImplTest 6 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 403：打卡记录查询结果补齐距离、结果状态、失败原因、经纬度字段，VO、Mapper 与返回链路现已完整带出记录解释信息。
- 2026-03-18 attendance 闭环 404：员工端打卡记录页展示增强，当前页现可直接查看本次打卡状态值、距离、失败原因和经纬度，不再只看成功/失败提示文案。
- 2026-03-18 attendance 闭环 405：管理端/列表端打卡记录展示增强，考勤列表现已补齐结果标签、距离、失败原因与坐标字段，对异常打卡状态可直接识别。
- 2026-03-18 attendance 闭环 406：统一打卡结果状态值，前后端已对齐使用 LOCATION_NOT_CONFIGURED、OUT_OF_RANGE、CHECK_IN_SUCCESS 等明确状态常量，不再只靠 message 文案判断。
- 2026-03-18 attendance 闭环 407：补齐未配置打卡点时的最小引导，员工打卡页会明确提示联系管理员前往“单位管理 > 打卡点设置”完成配置；AttendanceServiceImplTest 6 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 408：打卡记录查询新增 checkInStatus 筛选参数，后端 QueryRequest、Mapper 条件与 AttendanceServiceImplTest 已同步补齐，现可按结果状态过滤记录列表。
- 2026-03-18 attendance 闭环 409：员工/管理共用的考勤页新增“结果状态”筛选入口，切换状态后会直接刷新列表，并在列表头展示当前筛选口径。
- 2026-03-18 attendance 闭环 410：考勤记录导出补齐状态值、状态文案、距离、失败原因与经纬度字段，当前以前端 CSV 导出承接，导出口径与列表完全一致。
- 2026-03-18 attendance 闭环 411：异常打卡记录展示优化，列表已统一复用前端状态映射常量，对失败类状态补齐“异常”标识，列表与导出文案口径保持一致。
- 2026-03-18 attendance 闭环 412：补齐打卡记录筛选与导出联调文档，已记录状态枚举、筛选参数、导出字段以及未配置打卡点、超出范围等异常场景说明。
- 2026-03-18 attendance 闭环 413：新增打卡统计接口 /api/attendance/summary，后端现可返回总数、成功数、异常数以及各状态数量分布。
- 2026-03-18 attendance 闭环 414：打卡统计接口复用 AttendanceQueryRequest 并接入 tree_path 数据权限，现已支持按日期范围统计，且与当前权限模型保持一致。
- 2026-03-18 attendance 闭环 415：考勤页新增打卡统计卡片，管理端现可直接查看总记录数、成功数和异常数，不再只看明细列表。
- 2026-03-18 attendance 闭环 416：考勤页补齐异常率展示与状态分布标签，异常数已突出显示，页面可一眼识别整体异常情况。
- 2026-03-18 attendance 闭环 417：补齐打卡统计与异常监控联调文档，已记录统计接口、字段含义和状态口径；AttendanceServiceImplTest 9 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 418：新增异常用户榜单接口 /api/attendance/abnormal-monitor，后端已支持按 user_id 聚合异常次数、总记录数与异常率，并返回 Top10 排行。
- 2026-03-18 attendance 闭环 419：考勤页新增异常用户 Top10 榜单展示，管理端现可直接查看异常次数最高的用户排行以及对应异常率。
- 2026-03-18 attendance 闭环 420：异常用户榜单支持点击回筛，点击用户后会自动把查询条件切到该用户并仅保留异常记录，直接落到现有明细列表。
- 2026-03-18 attendance 闭环 421：异常监控补齐异常类型分布统计，榜单接口现同步返回基于 check_in_result 的异常状态分布，前端已在榜单区展示异常类型数量标签。
- 2026-03-18 attendance 闭环 422：异常用户榜单补齐 CSV 导出能力，当前可直接导出 Top10 的排名、用户、异常次数、总记录数与异常率；AttendanceServiceImplTest 10 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 423：补齐“异常用户趋势按日期汇总”最小闭环，新增 /api/attendance/abnormal-trend 接口按 userId + 日期返回 abnormalCount、totalCount、abnormalRate；前端已在异常榜单下方接入最小趋势图，切换用户会自动刷新趋势，无数据展示空态；AttendanceServiceImplTest 11 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 424：补齐“异常用户详情摘要”最小闭环，新增 /api/attendance/abnormal-user-summary 接口按 userId 返回姓名、账号、单位、最近异常日期、最近异常类型与异常总次数；前端已在趋势区上方接入用户摘要卡片，点击不同异常用户会与趋势同步刷新，无摘要或无趋势时均展示空态；AttendanceServiceImplTest 12 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 425：补齐“异常趋势点击日期后联动回筛当天异常明细”最小闭环，前端趋势日期柱现支持点击回筛，点击某一天后会自动切到当前选中用户的当天异常记录，再次点击已选日期可恢复原时间范围；趋势区已补当前选中日期高亮与联动日期提示，无数据保持空态；AttendanceServiceImplTest 12 项通过，npm run build 通过。全量 mvn test 仍受既有 orgtree Mockito/ByteBuddy attach 环境问题影响失败，与本次考勤改动无关。
- 2026-03-18 attendance 闭环 426：补齐“异常趋势区总览统计头部”最小闭环，前端已直接基于现有异常趋势数据计算异常总次数、总打卡次数、平均异常率与异常天数，并在趋势图上方展示；切换异常用户时总览、摘要、趋势与明细保持一致，点击趋势日期联动当天明细时，总览与摘要仍锁定原趋势范围，不受当天过滤串扰；AttendanceServiceImplTest 12 项通过，npm run build 通过。全量 mvn test 仍受既有 orgtree Mockito/ByteBuddy attach 环境问题影响失败，与本次考勤改动无关。
- 2026-03-18 attendance 闭环 427：补齐“异常榜单增加时间范围说明与口径提示”最小闭环，前端已在异常用户 Top10 区头部展示当前统计时间范围与异常判定口径提示；切换查询时间范围后说明文本会自动刷新，点击趋势日期联动当天明细时榜单仍复用原统计范围，不受当天过滤串扰；AttendanceServiceImplTest 12 项通过，npm run build 通过。全量 mvn test 仍受既有 orgtree Mockito/ByteBuddy attach 环境问题影响失败，与本次考勤改动无关。
- 2026-03-18 attendance 闭环 428：异常用户 Top10 榜单补齐当前选中用户高亮与显式说明，当前异常联动目标已可在榜单中稳定识别，减少切换用户时的上下文丢失。
- 2026-03-18 attendance 闭环 429：异常用户榜单支持再次点击当前用户直接取消联动并恢复原筛选条件，异常监控回筛现已具备最小可逆能力，不必只能依赖手动重置查询。
- 2026-03-18 attendance 闭环 430：异常监控补齐“清除异常联动”快捷入口，榜单区与空态区现都可一键恢复到联动前筛选，不必反复手动回退用户与日期条件。
- 2026-03-18 attendance 闭环 431：考勤列表头部补齐当前异常联动上下文摘要，现可直接看到当前是按哪位异常用户、哪一天进入明细列表，降低联调误判。
- 2026-03-18 attendance 闭环 432：异常联动空态补齐一键恢复入口，当前用户或当天异常无数据时可直接回到联动前范围；AttendanceServiceImplTest 12 项通过，npm run build 通过。
- 2026-03-18 attendance 闭环 433：异常榜单 CSV 导出文件名补齐当前选中用户与统计时间范围标识，导出文件离开页面后仍能直接判断对应联动上下文。
- 2026-03-18 attendance 闭环 434：异常榜单 CSV 导出补齐头部元信息，当前统计范围、异常口径与当前选中异常用户已直接写入导出内容，脱离页面也能独立理解导出口径。
- 2026-03-18 attendance 闭环 435：异常用户趋势头部补齐当前趋势统计范围说明，切换查询时间后趋势区现可直接确认摘要、总览与趋势图共用的统计口径。
- 2026-03-18 attendance 闭环 436：趋势区补齐“清除当天联动”快捷入口，当前从趋势点进入当天异常明细后可直接回到原时间范围，不必再依赖重复点击日期柱恢复。
- 2026-03-18 attendance 闭环 437：趋势区补齐“当天明细与原趋势范围关系”提示，当前点击某一天后页面会明确说明明细已切到当天，但趋势、摘要与总览仍按原统计范围计算，减少口径误读。
- 2026-03-18 attendance 闭环 438：考勤记录导出文件名补齐当前用户、结果状态与查询时间范围标识，导出文件离开页面后也能直接判断对应筛选上下文。
- 2026-03-18 attendance 闭环 439：考勤记录 CSV 导出补齐头部元信息，当前查询范围、状态筛选、筛选摘要与联动摘要已直接写入导出内容，脱离页面仍可独立验收口径。
- 2026-03-18 attendance 闭环 440：考勤列表头部补齐当前查询时间范围说明，进入列表后可直接确认当前记录是基于哪段时间范围查询出来的。
- 2026-03-18 attendance 闭环 441：考勤列表头部补齐筛选摘要，当前是否仅看异常、是否锁定某个用户或组织已可直接看到，减少联动后的口径误读。
- 2026-03-18 attendance 闭环 442：考勤列表空态补齐当前查询范围与筛选摘要提示，当前无数据时也能直接判断是“真没数据”还是“被当前筛选条件过滤掉了”。
- 2026-03-18 unit 闭环 443：接入真实高德地图 Key，前端已通过 `frontend/.env.local` 注入 `VITE_AMAP_KEY`，单位管理里的打卡点设置现会优先加载真实地图 SDK 并替换轻量选点模式；现有动态加载、地图初始化、点击选点、Geocoder 反查、marker 回显与未配置 Key 兜底逻辑继续复用，npm run build 通过。
- 2026-03-18 unit 闭环 444：打卡点地图区域放大为易操作模式，单位管理弹窗内的真实地图与轻量选点容器已同步提升到更适合点击、拖拽与缩放的尺寸；地图初始化与弹窗打开后会额外触发 `mapInstance.resize()`，避免放大后出现白屏、错位或半屏未渲染，现有重新定位、点击选点、已有经纬度回显 marker 与保存流程保持不变，npm run build 通过。
- 2026-03-18 unit 闭环 445：修复页面内地图区域未显示正式高德地图的问题，根因是正式地图容器此前被 `v-if=\"state.mapSdkReady\"` 反向阻塞，导致 `new AMap.Map(...)` 初始化时没有可挂载节点；现已改为检测到地图模式为 `sdk` 时先渲染真实地图容器，并在加载中显示占位层，页面内地图现可正常进入正式高德地图模式，继续支持拖拽、缩放、点击选点、marker 回显、地址反查与无 Key 兜底，npm run build 通过。
- 2026-03-18 unit 闭环 446：打卡点地图区域补齐缩放与当前位置可见化控件，真实地图模式现已在地图上提供 +/- 缩放按钮与缩放提示，弹窗内同步新增“当前选点摘要”卡片，明确当前是否已落点、经纬度与地址回填状态；点击地图选点、重新定位、已有经纬度回显三种场景共用现有 `locationForm` 与 marker 状态，轻量模式也补了对应交互提示，npm run build 通过。
- 2026-03-18 unit 闭环 447：打卡点地图区域补齐半径圆形覆盖可视化，真实地图模式现已基于当前 `longitude / latitude / radiusMeters` 绘制 `AMap.Circle` 预览打卡范围；半径输入变化、点击地图选点、重新定位与已有经纬度回显时圆形覆盖都会同步刷新，经纬度或半径缺失时会自动隐藏避免误导，当前选点摘要也已补充“当前打卡范围半径”说明，轻量模式同步提示正式地图可预览打卡半径圆，npm run build 通过。
- 2026-03-18 unit 闭环 448：打卡点地图区域补齐半径标签与视野自适应，真实地图模式现已基于当前 `longitude / latitude / radiusMeters` 同步绘制 `AMap.Text` 范围标签，并在半径有效时自动用 `setFitView` 让 marker 与打卡范围圆完整进入视野；半径变化、点击地图选点、重新定位与已有经纬度回显都会同步刷新标签和视野，经纬度或半径缺失时会自动隐藏标签并回到仅以选点为中心的默认视图，摘要区与轻量模式文案也已补充视野说明，npm run build 通过。
- 2026-03-18 unit 闭环 449：打卡点地图区域补齐 marker 拖拽微调选点，真实地图模式现已把 `AMap.Marker` 调整为可拖拽，用户可在已有选点基础上直接拖动 marker 做小范围微调；拖拽结束后会自动同步 `longitude / latitude`、触发 Geocoder 反查地址，并继续复用现有 marker、半径圆、范围标签、摘要区与视野自适应刷新链路，轻量模式同步提示正式地图支持拖拽微调，npm run build 通过。
- 2026-03-18 unit 闭环 450：打卡点地图区域补齐地址搜索并定位候选点，真实地图模式现已提供最小可用的“地址搜索 + 搜索定位”入口，并直接复用高德 Geocoder 正向地理编码把地址解析为候选点；搜索成功后会同步更新 marker、`longitude / latitude`、地址、半径圆、范围标签、摘要区与视野状态，搜索失败时仅提示失败且保留当前已选点，轻量模式同步提示正式地图支持地址搜索定位，npm run build 通过。
- 2026-03-18 unit 闭环 451：打卡点地址搜索升级为候选列表模式，真实地图模式现会把 Geocoder 返回结果收口为最多 5 条候选并展示名称/格式化地址；回车或点击搜索按钮时仍会默认快速选用首项，点击任一候选也会继续复用现有 `fillLocation()` 刷新 marker、经纬度、地址、半径圆、范围标签、摘要区与视野状态，搜索失败时保留当前已选点且不污染候选列表，轻量模式同步提示正式地图支持地址搜索候选定位，npm run build 通过。
- 2026-03-18 unit 闭环 452：打卡点地址搜索候选列表补齐体验收口，当前候选列表已增加“已选用”高亮态，用户可直接识别当前点来源于哪条候选；输入框内容变更后会自动清空旧候选，点击地图、重新定位、已有经纬度回显、marker 拖拽与保存后同步也都会主动清空旧候选，避免脏状态误导；回车与搜索按钮仍默认快速选用首项，没有新增任何地图服务调用频率，轻量模式提示也已明确候选搜索仅正式地图模式可用，npm run build 通过。
- 2026-03-18 backend 闭环 453：修复单位打卡点查询接口在 Spring Boot 3.x 下的参数绑定报错，当前 `UnitController` 已为 `/{id}` 路由显式补齐 `@PathVariable(\"id\")`，同时在 Maven 编译器中开启 `parameters=true`，避免运行时再依赖方法参数名推断；后端执行 `mvn -DskipTests compile` 通过，打开地图时的 `Name for argument of type [java.lang.Long] not specified` 问题已完成最小侵入修复。
- 2026-03-18 unit 闭环 454：排查并修复高德地图已初始化但底图瓦片未显示的问题，当前前端已在 `amap.js` 中补齐最小调试日志，明确输出 key/security 配置、SDK 回调与脚本加载状态；地图初始化时也已强制恢复默认 `TileLayer`、`mapStyle=amap://styles/normal` 与 `viewMode=2D`，避免因空白底图层或错误样式导致页面只剩浅蓝背景；npm run build 通过，如浏览器控制台继续提示未配置 `VITE_AMAP_SECURITY_JS_CODE`，则可直接定位为安全码缺失而非地图实例初始化失败。
- 2026-03-18 unit 闭环 455：进一步诊断并修复高德底图资源层，当前已把 `window._AMapSecurityConfig.securityJsCode` 注入前移到 SDK script 挂载前的显式步骤，并在 `amap.js` 中补齐“仅输出是否存在”的最小调试日志；地图初始化完成后也会再次执行默认底图层恢复，显式清理异常 `mapStyle` 并重设默认 `TileLayer`，避免 SDK 已起但底图层仍停留在空白状态；npm run build 通过，浏览器侧可通过 console 中的 `[AMap]` 与 `[UnitMap]` 日志，以及 network 中高德瓦片请求状态，继续确认是否仍受安全码或白名单配置影响。
- 2026-03-18 param 闭环 456：修复参数管理页“保存成功但列表不显示”的假成功问题，当前前端参数页已补齐 `ApiResponse.code` 成功校验；参数列表、保存、删除、启停四条链路在 `code != 0` 时都会直接抛出真实后端消息，不再把业务失败误判成保存成功并把空数据当成正常列表结果；npm run build 通过。后续若参数仍保存失败，页面现在会直接显示真实报错，便于继续定位具体表结构或权限问题。
- 2026-03-18 unit 闭环 457：地图配置已改为从参数管理动态读取，后端新增 `GET /api/config/amap` 从 `sys_param` 中读取已启用的 `Key` 与 `securityJsCode`，缺失时返回明确错误；前端 `amap.js` 已停止使用 `.env` 中的 AMap 配置，改为先请求后端配置，再在 SDK script 加载前注入 `window._AMapSecurityConfig.securityJsCode`，并保留 `[AMap] config loaded / security injected / SDK loaded / map initialized` 最小调试日志；单位页地图初始化继续强制恢复默认底图层与 `amap://styles/normal`，marker、半径圆、标签、搜索、拖拽与保存流程保持不变；后端 `mvn -DskipTests compile` 与前端 `npm run build` 均已通过。
- 2026-03-18 unit 闭环 458：针对“地图实例正常但底图瓦片未显示”的场景进一步收口底图资源层，当前单位页地图初始化已改为显式创建 `new AMap.TileLayer()` 并在 `new AMap.Map(...)` 后通过 `map.add(layer)` 强制补回默认底图，同时立即执行 `map.setMapStyle('amap://styles/normal')`，不再使用可能覆盖默认底图的 `setLayers(...)` 恢复路径；前端全局已确认不存在 `clearMap()`、`setLayers([])` 或会误删默认底图的 `remove(...)` 操作，并保留 `[UnitMap] base layer ensured` 调试日志；marker、半径圆、标签、拖拽、搜索与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 459：确认业务页底图不显示属于页面代码/样式问题而非高德配置问题，当前已在 `UnitListView.vue` 中收口正式地图容器样式，显式避免 `scoped` 样式或容器背景误伤高德内部 `.amap-maps / .amap-layers / .amap-layer / .amap-tile / canvas / img`；正式地图容器已改为透明背景，并通过 `:deep(...)` 强制恢复高德底图层的可见性、透明度与尺寸，避免业务页仅剩浅色背景而底图 DOM 被隐藏；marker、半径圆、标签、搜索、拖拽与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 460：开始从业务页地图实例生命周期排查底图渲染状态，当前 `UnitListView.vue` 已补齐单实例控制、弹窗关闭时的 `map.destroy()` 清理、reopen 时强制重新挂载地图容器 `key`，并把地图初始化改为“容器真正可见且尺寸稳定后再执行”；初始化后会立即和延迟各执行一次 `map.resize()`，同时输出容器宽高、`getZoom()`、`getCenter()`、`getLayers()` 数量，以及 `complete / tilesloaded` 阶段日志，便于确认底图瓦片是否实际进入渲染；marker、半径圆、标签、搜索、拖拽与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 461：业务页地图初始化时机已从 `openLocationDialog()` 提前阶段改为 `van-popup` 的 `@opened` 阶段，当前单位打卡点弹窗会在动画真正完成、容器已可见后，依次执行 `nextTick -> waitForMapContainerVisible -> ensureMapReady -> ensureMapResized`；同时补了 `300ms` 与 `800ms` 两次延迟 `resize()` 兜底，避免弹窗动画期间底图瓦片尚未进入正确渲染区域；`openLocationDialog()` 已删除过早初始化地图的代码，marker、半径圆、标签、搜索、拖拽与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 462：业务页地图已去掉手工追加 `TileLayer` 的修复逻辑，当前 `UnitListView.vue` 已回退到与独立测试页一致的最小地图初始化方式，只保留 `new AMap.Map(...) + zoom + center + viewMode + mapStyle: 'amap://styles/normal'`；原先 `new AMap.TileLayer()`、`map.add(baseTileLayer)` 与 `complete` 回调中的重复补底图层逻辑已全部删除，marker、半径圆、范围标签、搜索、拖拽、保存流程以及 popup opened / resize 时机修复保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 463：单位打卡点地图默认中心点已统一切换为指定经纬度 `[118.091519, 24.478829]`，当前 `UnitListView.vue` 已收口为常量 `DEFAULT_MAP_LONGITUDE / DEFAULT_MAP_LATITUDE`；地图初始化 `center` 与轻量选点模式 `handleMapSelect()` 中的无经纬度兜底基准坐标均已同步替换，当前单位若已有打卡点仍优先回显已有经纬度，只有无已有点位时才落到新的默认中心；搜索、拖拽、半径圆、标签与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 464：针对“业务页首次打开底图不渲染、需交互后才显示”的问题补齐首帧视图刷新，当前 `UnitListView.vue` 已在地图 `complete` 之后主动执行一次最小视图刷新，并在 `100ms` 与 `300ms` 各补一次延迟刷新；刷新中心遵循“已有打卡点经纬度优先，否则回退到默认中心 `[118.091519, 24.478829]`”，并优先使用 `map.setZoomAndCenter(currentZoom, currentCenter)`，否则退回 `setCenter + setZoom`；marker、半径圆、范围标签、搜索、拖拽、定位与保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 465：打开单位打卡点弹窗时已支持“无历史点位则直接写入默认经纬度到表单，并主动触发一次定位式地图刷新”，当前 `UnitListView.vue` 在无已配置经纬度时会立即通过 `fillLocation()` 写入默认值 `118.091519 / 24.478829`，并补一个最小地址占位“默认地图中心点”；若已有历史点位则继续优先用历史经纬度回显。地图初始化完成后会主动按当前表单经纬度执行 `syncSdkMarkerFromForm() + refreshInitialViewport()`，从而同步回显 marker、半径圆、范围标签和地图中心，不再依赖用户手点“定位当前位置”来把底图刷出来；搜索、拖拽、保存流程保持不变，`npm run build` 通过。
- 2026-03-18 unit 闭环 466：复核 `UnitListView.vue` 内所有打卡点默认半径 fallback，当前 `state.locationForm.radiusMeters` 初始值、`openLocationDialog()` 无历史数据默认值、`queryUnitAttendanceLocation` 回填 fallback、`handleLocationSubmit` 提交前 fallback，以及保存后详情同步 fallback 已全部统一为 `100`；文件内已不存在默认半径相关的 `200` 兜底。若页面当前仍显示 `200`，说明该单位历史已保存半径就是 `200`，前端仍按“历史值优先于默认值”正常回显；`npm run build` 通过。
- 2026-03-19 attendance 闭环 467：补齐考勤查询日期范围校验，当前 `AttendanceCheckInView.vue` 已在查询与两类导出前统一拦截“开始日期晚于结束日期”，避免页面误显示为空结果；后端 `AttendanceServiceImpl` 也已统一校验 `dateFrom/dateTo` 并抛出明确提示“开始日期不能晚于结束日期”，防止手工调接口绕过前端；`AttendanceServiceImplTest` 13 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 468：考勤查询区新增“今天 / 近7天 / 本月”快捷时间范围筛选，当前 `AttendanceCheckInView.vue` 点击后会自动回填开始日期与结束日期并直接触发现有查询链路；列表、异常监控、趋势范围、导出文件名与头部元信息继续复用既有日期字段，不新增后端改动；快捷范围也兼容现有日期范围校验，`npm run build` 通过。
- 2026-03-19 attendance 闭环 469：补录表单的用户选择方式已优化为优先按姓名或手机号搜索，当前 `AttendanceCheckInView.vue` 已接入复用 `/api/users` 的最小用户搜索链路；搜索后可直接点选候选用户，并自动回填补录所需的 `userId`，同时展示姓名、账号、手机号、组织和用户ID摘要；未选择用户时仍沿用现有逻辑默认补录到当前登录用户，保存接口与现有校验链路未改动，`npm run build` 通过。
- 2026-03-19 attendance 闭环 470：考勤列表补齐最小分页闭环，后端 `AttendanceQueryRequest` 已新增 `pageNo/pageSize` 并返回 `AttendancePageVO`，`AttendanceMapper.xml` 查询已按 `LIMIT/OFFSET` 分页；前端 `AttendanceCheckInView.vue` 已接入 `van-pagination`、页码摘要与查询条件变化后回到第一页，删改或筛选后落到空页时也会自动回退到上一有效页；统计、异常监控、趋势与导出仍复用原筛选条件，不扩散改动；`AttendanceServiceImplTest` 14 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 471：优先完成“签到管理-地图定位打卡稳定性专项收口”的前端最小修复，当前 `UnitListView.vue` 已统一地图默认中心点/默认缩放/默认半径常量，弹窗首次打开会在容器可见后再初始化并仅做首轮底图稳定刷新，不再用 `tilesloaded` 反复抢回用户视图；SDK 定位失败时会稳定回退到浏览器定位，再失败则保留当前选点或默认中心点并给出明确提示。经纬度/半径字段手工修改后，marker、半径圆、范围标签与视野会继续同步刷新，搜索、拖拽、保存口径保持一致；`AttendanceCheckInView.vue` 也已把打卡成功/失败后的列表与当前打卡点信息统一回刷，避免查询列表与打卡结果回显脱节；`amap.js` 补齐脚本单例与复用逻辑，减少弹窗重复打开时的 SDK 脚本竞态；`npm run build` 通过。真实浏览器人工专项验收仍建议按“首次打开底图 / 定位成功失败 / 搜索拖拽保存 / 打卡后列表回刷”清单再走一轮。
- 2026-03-19 attendance 闭环 472：继续按“真实浏览器专项验收”口径收紧地图定位打卡的真实场景提示与稳定性，当前 `UnitListView.vue` 已把浏览器定位不可用与定位失败两条兜底提示区分为“保留当前选点”或“回退默认中心点”两种文案，避免人工验收时因提示与实际地图状态不一致而误判；连续开关弹窗、默认半径/中心点/缩放、搜索/拖拽/手工修改经纬度与半径、打卡后列表与当前打卡点信息回刷链路继续沿用闭环 471 的统一状态同步逻辑；仓库当前仍无 Playwright/Cypress 等现成浏览器自动化脚手架，本轮验证方式仍为代码侧收口 + `npm run build` 通过，后续真实浏览器人工验收可直接对照 5 组场景逐项走查。
- 2026-03-19 attendance 闭环 473：补齐“打卡异常与乱码修复”最小闭环，定位到真实问题主要有三处：一是 `AttendanceCheckInView.vue` 的 `handleCheckIn()` 之前会把接口异常统一误写成“定位失败，请重试”，导致浏览器 Network 中后端真实报错被前端覆盖；二是 `AttendanceServiceImpl` 对打卡点经纬度/半径缺失没有友好兜底，异常配置会退化成底层异常；三是后端 Maven 与 Servlet 编码此前未显式固定 UTF-8，启动日志会提示 platform encoding，存在中文提示被错误编译或传输解释的风险。当前已在后端把打卡点配置不完整收口为明确中文提示“当前单位打卡点配置不完整，请联系管理员重新保存”，`GlobalExceptionHandler` 也改为优先返回非空友好中文文案；同时在 `application.yml` 与 `pom.xml` 显式固定 UTF-8，前端 `handleCheckIn()` 则拆分为“浏览器定位异常”和“接口请求异常”两条链路，不再吞掉后端真实 message，也避免接口失败时再次误报成定位异常。验证结果：`AttendanceServiceImplTest` 14 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 474：继续围绕闭环 473 做“打卡成功/失败真实浏览器验收收口”，本轮只补了两个真实残留点：其一，`AttendanceCheckInView.vue` 在接口异常响应分支现会根据后端中文 message 反推出更贴近真实场景的状态值，例如“当前单位打卡点配置不完整，请联系管理员重新保存”会稳定落到 `LOCATION_NOT_CONFIGURED`，避免结果区只剩失败原因但没有可读状态；其二，接口异常分支现在也会和业务失败分支一样主动回刷考勤列表与当前打卡点信息，减少“打卡失败后结果区变了，但列表/当前打卡点卡片还是旧状态”的验收落差。未新增后端功能，验证结果为 `npm run build` 通过。
- 2026-03-19 attendance 闭环 475：完成“签到管理-验收前总收口”的最小整理，本轮未新增任何业务功能，只补了一份简明的 `ai_docs/modules/module_attendance.md` 验收说明，集中整理签到模块当前已完成能力、真实验收场景清单，以及“打卡成功 / 业务失败 / 接口异常”三类分支在结果区、考勤列表、当前打卡点卡片上的一致性结论；同时按当前工作区状态把未提交改动归类为“考勤分页与日期范围校验 / 打卡异常与 UTF-8 编码修复 / 地图定位打卡稳定性专项 / 进度记录”四组，避免多轮闭环混杂后难以验收或提交。代码侧复核确认：`AttendanceCheckInView.vue` 在成功分支、业务失败分支与有服务端响应的接口异常分支均会统一回刷 `fetchList()` 与 `fetchCurrentLocation()`，当前无需再追加前端逻辑补丁；验证结果为 `npm run build` 通过。
- 2026-03-19 attendance 闭环 476：完成“签到管理-最终人工验收与提交整理”的最小收口，但本轮结论已如实区分“当前环境可验证项”和“真实浏览器人工待验项”。仓库内仍未接入 Playwright、Cypress、Puppeteer 等现成浏览器自动化脚手架，当前 CLI 环境也无法代替人工逐项操作浏览器，因此 `module_attendance.md` 已明确记录：本轮不能诚实标记为“已逐项完成真实浏览器人工验收”；不过代码层主链路已再次复核，打卡成功、业务失败、以及“有服务端响应”的接口异常三类分支，都会统一回刷结果区关联数据、考勤列表与当前打卡点卡片，不存在新的阻塞性不一致。提交整理方面，当前 attendance 相关未提交改动已继续维持为 4 组边界：考勤分页与日期范围校验、打卡异常与 UTF-8 编码修复、地图定位打卡稳定性专项、验收与进度文档，避免后续混杂提交。验证结果：`AttendanceServiceImplTest` 15 项通过，`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 477：在异常用户 Top10 的基础上补齐“风险评分”最小闭环，当前 `AttendanceAbnormalUserRankVO` 已新增 `riskScore / riskLevel`，并由 `AttendanceServiceImpl` 基于异常次数与异常率统一计算；前端异常榜单已直接展示风险分和高/中/低风险等级，领导查看 Top10 时不再只能靠异常次数主观判断严重程度。验证结果：`AttendanceServiceImplTest` 16 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 478：补齐“最近7天趋势 + 上升/下降”最小闭环，后端新增按用户聚合最近7天与前7天异常次数的对比查询，并在异常榜单与用户摘要中统一输出 `recent7DayAbnormalCount / previous7DayAbnormalCount / trendDirection`；前端现已直接展示“上升 / 持平 / 下降”的趋势判断，便于汇报时说明异常是否正在恶化。验证结果：`AttendanceServiceImplTest` 16 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 479：补齐“异常原因分布增强”最小闭环，当前异常监控接口已返回失败原因 Top 分布与用户级主要异常原因，并在 service 中统一收口中文标签与原因分类标签；前端异常监控区已新增原因分布列表，同时榜单卡片可直接看到每位异常用户的“主要异常原因 / 原因标签”，支持从次数视角切到原因视角。验证结果：`AttendanceServiceImplTest` 16 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 480：补齐“异常用户画像”最小闭环，后端已在异常用户摘要中增加地点集中度、最高频地点、上午/下午/晚间异常次数与高发时段分析；前端用户摘要卡片现可直接展示地点集中度和时间段画像，便于领导汇报时描述“异常集中发生在哪、发生在什么时段”。验证结果：`AttendanceServiceImplTest` 16 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 481：补齐“异常预警机制”最小闭环，当前 `AttendanceServiceImpl` 已基于“高风险 / 近7天异常>=3 / 持续上升且异常率高”三条简单规则触发预警，并把 `alertTriggered / alertRuleText` 下发到异常榜单与用户摘要；前端异常 Top10 区已同步展示预警人数、单用户预警标识和命中规则说明，异常用户智能分析体系现已具备可直接用于领导汇报的“风险 + 趋势 + 原因 + 画像 + 预警”闭环。验证结果：`AttendanceServiceImplTest` 16 项通过，`npm run build` 通过。
- 2026-03-19 attendance 闭环 482：完成“异常用户分析模块-验收前收口”的最小整理，本轮未新增分析能力，只补了一份专项文档 `module_attendance_abnormal.md`，把风险评分、趋势分析、原因分布、用户画像、预警规则的统计口径、字段说明与提交边界集中写清；同时复核并微调了 `AttendanceCheckInView.vue` 中异常监控区的信息层级与空态，把“异常原因分布”提升为独立说明块，并补齐无分布数据时的明确提示，避免验收时误判为加载缺失。提交边界方面，本轮已明确 477-481 仅包含“异常分析后端能力 / 异常分析 VO / 异常监控前端最小展示 / 测试适配 / 文档与进度”五组文件，不与签到地图专项混杂。验证结果：`npm run build` 通过。
- 2026-03-19 attendance 闭环 483：完成“异常用户分析模块-提交与演示收口”的最小整理，本轮继续暂停新增分析能力，只围绕提交说明、演示顺序和展示一致性做最后收口；`module_attendance_abnormal.md` 现已补齐“提交顺序建议 / 演示顺序建议”，明确 477-482 应按“后端分析能力 -> VO 与测试适配 -> 前端最小展示 -> 文档与进度”顺序整理提交。前端 `AttendanceCheckInView.vue` 则把用户摘要中的预警展示统一为“预警状态 / 预警规则”两级标签，和榜单区的风险、趋势、原因、预警信息形成更稳定的演示话术。验证结果：`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 484：完成“异常用户分析模块-分组提交收口”的最小整理，本轮继续暂停新增功能，只围绕 477-482 的 4 组提交边界做最后确认；`module_attendance_abnormal.md` 现已补齐每组提交的应含文件、建议提交说明文本，以及当前工作区必须排除的签到地图、分页、打卡异常编码等无关改动。经当前工作区核对，异常分析有效文件仍可稳定收敛为“后端分析能力 / VO 与测试适配 / 前端最小展示 / 文档与进度”4 组，不与 `amap.js`、`UnitListView.vue`、`AttendancePageVO.java` 等既有非异常分析改动混杂。验证结果：`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 485：完成“打卡定位超出范围问题排查”最小收口，先梳理出当前链路为“前端浏览器 `navigator.geolocation` 取当前坐标 -> `AttendanceCheckInView.vue` 提交 `/api/attendance/check-in` -> 后端 `AttendanceServiceImpl` 用数据库中的单位打卡点经纬度和半径做 Haversine 距离校验 -> 超半径时返回 `OUT_OF_RANGE`”；排查结果确认此前前端未本地诊断距离、未上送 `accuracy`，且浏览器原生定位坐标与单位打卡点配置地图使用的高德坐标存在 `WGS84 / GCJ-02` 混用风险，容易出现“地图上看着在范围内，提交后仍判超出范围”。本轮最小修复为：前端打卡前同时计算浏览器原始坐标与 GCJ-02 转换后坐标到当前打卡点的本地距离，自动选择更接近打卡点的一组坐标提交，并在控制台打印原始坐标、转换后坐标、目标打卡点、半径、accuracy、本地距离和实际提交坐标；后端 `CheckInRequest` 新增 `accuracyMeters`，`AttendanceServiceImpl` 补齐接收日志与判定日志，统一打印接收坐标、目标打卡点坐标、半径、accuracy、最终距离和判定结果；超出范围提示也改为“当前位置距离打卡点 X 米，允许半径 Y 米”，当 `accuracy` 大于半径时追加“当前定位精度约 Z 米，请尽量在室外空旷区域重试”。这样当前前端提交经纬度已明确来自“浏览器最新定位值与 GCJ-02 对齐后更贴近打卡点的一组坐标”，后端校验经纬度明确来自数据库 `attendance_location`，距离计算仍统一只在前端诊断和后端最终校验两层使用同一套 Haversine 公式；此前误报的主要原因是浏览器原始坐标与高德地图配置点坐标系不一致且页面缺少 `accuracy` 诊断，现在通过“最新定位值直提 + 坐标系对齐 + 前后端同口径日志 + 更可读提示”降低再次误判的概率。验证结果：`AttendanceServiceImplTest` 17 项通过，`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 486：完成“室内打卡定位失真误判修复”最小收口，重点解决“无效定位/粗定位被误报成超出范围”。本轮先按链路确认当前流程为“前端浏览器定位采集 -> `accuracyMeters` 上送 -> 后端距离校验 -> 返回状态值与原因”，然后仅在考勤打卡链路中新增 `LOCATION_INVALID / LOCATION_WEAK` 两类状态，把“定位无效”“定位质量差”“真实超范围”三种情况彻底拆开：当坐标缺失、为零点或 `accuracy` 极大时，后端不再判 `OUT_OF_RANGE`，而是直接返回 `LOCATION_INVALID` 与“请检查定位权限或定位服务”提示；当 `accuracy` 超过稳定打卡阈值时，返回 `LOCATION_WEAK` 与“室内信号弱、建议等待稳定或靠窗”提示；只有定位有效且质量达到最低要求时，才进入真实超范围判定。前端 `AttendanceCheckInView.vue` 同步补齐连续 3 次定位采样、明显无效点过滤、最优定位结果选择、以及按当前打卡点半径判断定位质量的最小策略，并继续保持 `enableHighAccuracy` 打开；为提升室内打卡成功率，后端还增加了基于 `accuracy` 的最小容错，当距离只略微超出半径且仍处于可接受精度范围时允许按室内弱漂移放行，同时保留了后续继续接入 Wi‑Fi / Beacon / 场所辅助判定的扩展空间。最小验证结果已覆盖三类关键场景：超大精度粗定位不再误报“超出范围”，而是返回 `LOCATION_INVALID`；室内弱信号会返回 `LOCATION_WEAK` 而不是 `OUT_OF_RANGE`；真实超范围仍继续被正常拦截；同时轻微室内漂移在容错范围内可正常打卡成功。验证结果：`AttendanceServiceImplTest` 19 项通过，`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 487：完成“打卡定位双圆可视化 + 弱定位容错判定”最小闭环，当前 `AttendanceCheckInView.vue` 已在“本次打卡结果”下方新增定位可视化模块，使用现有高德 SDK 在签到页直接展示红色打卡点 marker、红色允许范围圆、蓝色当前定位 marker、蓝色定位精度圆，以及两点连线；地图会按当前打卡点和最新定位样本自动 `fitView`，保证两个点和两个圆尽量完整进入视野。页面同时新增前端诊断字段，展示当前定位经纬度、打卡点经纬度、允许半径、accuracy、当前距离、重叠判定 `distance <= allowedRadius + accuracyMeters`、后端返回判定结果，以及 `decisionBranch / weakToleranceApplied`，其中重叠仅作为辅助展示，不直接参与最终放行。后端 `AttendanceServiceImpl` 则按“INVALID -> SUCCESS -> WEAK_SUCCESS -> LOCATION_WEAK -> OUT”的顺序重构了弱定位容错判定：当 `accuracy > 1000` 或经纬度异常时直接返回 `LOCATION_INVALID`；当 `distance <= allowedRadius` 时正常成功；当 `accuracy <= 80` 且 `distance <= allowedRadius + min(accuracy, 50)` 时命中弱定位容错成功，并通过 `decisionBranch=WEAK_SUCCESS / weakToleranceApplied=true` 回传；当 `accuracy` 处于 `(80, 1000]` 且未命中容错时返回 `LOCATION_WEAK`；其余场景才继续返回 `OUT_OF_RANGE`。前后端日志也已保留原始/转换后坐标、accuracy、distance、overlap、最终提交坐标以及后端分支名，能够完整回溯判定过程。最小验证结果：室内轻微漂移场景可通过弱命中打卡，超大 accuracy 不再误报超范围，明显超出范围继续被正确拦截，前端构建通过且地图模块代码已具备双圆重叠可视化能力。验证结果：`AttendanceServiceImplTest` 19 项通过，`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-19 attendance 闭环 488：完成“打卡定位采集稳定性修复”最小闭环，本轮继续暂停 Wi‑Fi、多因子、防作弊等后续能力，只围绕“打卡时拿不到可用定位 / 误用错误定位 / 提交旧定位”收口。当前 `AttendanceCheckInView.vue` 已调整为点击“立即打卡”时先清空上一次提交定位，再强制重新执行 3 次浏览器高精度采样，并为每次采样记录 `latitude / longitude / accuracy / timestamp`；采样完成后仅在本轮新样本中做质量筛选，明显无效点（经纬度缺失、越界、零点坐标、`accuracy > 1000`）会直接废弃，若三次采样都无效则前端不再继续业务提交流程，而是统一提示“未获取到可用定位，请检查定位权限或定位服务后重试”。为便于真实设备排查，本轮还补齐了定位环境与采样日志，前端会打印 `userAgent`、当前页面 URL、协议/主机、是否 HTTPS/localhost/内网、每次采样结果、最终采用结果、是否做 GCJ‑02 坐标转换，以及转换前后坐标；页面结果区则新增“定位阶段 / 是否已获取到可用定位”提示，并明确地图只展示最终提交定位点，不再继续显示与本次提交无关的旧样本。这样当前点击打卡时提交的经纬度已明确只来自“本次点击中新采样出的最终有效点”，若仍失败，控制台日志也足以区分是权限问题、环境问题还是样本质量问题。验证结果：`npm run build` 通过；Vite chunk size warning 按本轮要求未处理。
- 2026-03-20 weeklywork 闭环 259：周报管理页查询、重置与刷新链路补上忙碌态短路，提交中或详情加载中即使被重复触发也不会再并发拉取列表，减少筛选条件和列表结果在处理中途漂移。
- 2026-03-20 weeklywork 闭环 260：周报管理页“新建周报”入口补上编辑内容丢失确认，存在草稿上下文或未保存输入时不会再被一次误触直接清空。
- 2026-03-20 weeklywork 闭环 261：周报管理页从详情进入编辑前统一先收起详情弹层，避免编辑区与详情区同时悬挂造成上下文分裂。
- 2026-03-20 weeklywork 闭环 262：周报管理页详情入口命中当前已打开周报时直接短路，避免重复拉取同一条详情并减少无效刷新。
- 2026-03-20 weeklywork 闭环 263：周报管理页详情弹层关闭动作统一清理详情记录与加载态，避免下次打开前沿用旧详情残留。
- 2026-03-20 weeklywork 闭环 264：周报管理模块补齐逐级审批流底座模型，后端已从单级 `SUBMITTED/APPROVED/RETURNED` 升级为“科员 -> 科长（主任） -> 分管领导 -> 团长”的最小审批状态模型，新增当前审批节点、定向退回目标、退回意见、最近审批人/时间与审批日志表 SQL 文件；`review` 请求现支持 `returnTarget/comment`，退回动作强制填写意见并写入审批日志，详情接口同步返回可退回节点与审批记录，前端周报详情弹层已最小接入当前审批环节、最近退回信息与审批记录时间轴，退回按钮也已支持录入退回目标与意见；验证结果：`mvn -Dtest=WeeklyWorkServiceImplTest test` 通过，`npm run build` 通过。
- 2026-03-20 weeklywork 闭环 265：周报填写页完成移动端优先的轻卡片式重构，在保留现有保存、提交、编辑、详情逻辑不变的前提下，新增顶部状态头卡、四段式审批步骤条、单任务卡片视图、底部吸底操作栏和任务弹层编辑；“任务性质”已改成手机高频操作更友好的四个大号胶囊按钮，填写页的层级、留白和触控面积明显改善。当前最小闭环仍复用既有 `workPlan/workContent/remark` 提交字段，通过前端同步把任务卡草稿映射回原字段，不扩后端接口。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 266：周报详情弹层完成“审批操作区美化 + 退回弹层体验优化”最小闭环，审批区已改成更适合移动端的主次按钮布局：审核通过为主按钮、修改后通过为次主按钮、退回保留危险动作语义但样式更克制；退回流程已从 `prompt` 升级为底部弹层，显式展示当前审批节点、可退回目标按钮组选项、退回意见输入框，以及清晰的“取消 / 确认退回”操作区；修改后通过入口现会先给出“审批修改模式”确认提示，并在填写页头卡内继续显示修改模式提示。审批记录时间轴也已轻量美化，新增动作标签、处理人/时间/意见展示，退回记录会额外突出退回目标，且补充了提交/修改的前端时间轴视图。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 267：周报管理页完成一次“功能收口式重构”，当前页面已压缩为“当前周报区 / 当前周报任务列表区 / 周报记录区”三块核心业务区域，不再保留过渡性查询区、开发说明文案和“清空本页”等低价值入口。重点修复了提交后记录不显示的问题：列表刷新链路已从单纯复用 `fetchList()` 改成“强制刷新 + 本地回填 + 当前记录同步”，在暂存、提交、列表提交、审批退回/通过后都会显式刷新记录源，并在前端用 `upsertRecord()` 先把当前记录回填进记录区，避免出现“提交成功但列表空白”；同时记录区已按真实业务视角拆成“我的周报 / 待我审批 / 我已处理”，当前周报区与记录区关系更清晰，草稿/提交/退回/审批状态切换后的页面展示也更稳定。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 268：周报管理页继续完成“业务收口”最小闭环，当前 `WeeklyWorkEditView.vue` 已不再依赖隐藏查询条件拉列表，列表请求统一直接取真实记录，避免旧筛选条件把刚提交的周报挡掉；填写区也从“单任务过渡态”升级为前端多任务列表，使用 `TASKS_JSON::` 序列化兼容既有 `workPlan/workContent/remark` 字段，在不改后端接口的前提下支持同一份周报下新增、编辑、删除多项任务，并在详情、列表概览和任务数量展示中统一解析回显。与此同时，审批人的“修改后通过”链路已改为真实可进入审批修改模式，详情接口返回的真实记录也会反向回填到记录区，进一步收紧“提交/查看/审批/再编辑”闭环。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 269：周报管理页完成“记录区与审批区彻底对齐”最小闭环，当前记录区每条卡片已统一展示周次、任务数、当前状态、当前审批节点和最近更新时间，并优先以列表/详情返回的真实记录为准，不再混用当前编辑缓存。前端新增“当前选中记录”主线后，点击记录卡片即可稳定切换当前周报区和任务区，高亮态、详情弹层与审批区也都会围绕同一条记录同步刷新；当详情弹层已打开时，切换记录会同步更新详情数据，避免出现“列表选中 A、详情停留 B”的错位。操作入口也按角色与状态进一步收口：科员记录卡仅保留“查看 / 继续编辑 / 提交审核”三类入口，审批人待处理记录保留“审批处理”单一主入口并在详情中完成通过/退回/修改后通过，非当前处理节点记录则只读查看。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 270：周报管理页完成“当前周报区与编辑态彻底收口”最小闭环，当前页已新增统一的模式派生：新建本周周报 / 编辑周报 / 查看周报 / 审批处理中 / 审批修改模式，状态头卡、提示文案和底部主按钮区都由同一模式结果驱动，不再由零散布尔值拼接。前端同时补齐了编辑基线快照与统一未保存拦截：切换到新建、切换记录、进入其他编辑态、退出编辑态、退出审批修改模式前，都会先确认是否放弃当前未保存修改，避免 `activeRecordId`、当前编辑对象和任务弹层对象在无提示情况下错位。提交/暂存/审批通过/退回/修改后通过后也会同步回收或重置编辑态快照，保证当前区标题、按钮文案、任务区可编辑状态始终和当前选中记录保持一致。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 271：周报管理页完成“审批详情弹层与记录卡对齐 + 审批时间轴业务化”最小闭环，记录卡与详情弹层现已统一使用相同的状态、审批节点、任务数、最近更新时间和最近退回摘要口径，避免列表摘要与详情弹层出现两套文案。详情弹层头部也已收口为业务摘要卡，只保留周次、当前状态、当前审批节点、填报人、最近更新时间五类关键信息；审批记录时间轴则改成业务视图，当前每条记录都会展示动作中文标签、处理人、处理时间、审批意见，以及退回目标或流转去向，不再直接把日志字段当调试信息裸露给业务人员。审批操作区仍只在当前记录可审批时展示，并继续复用同一条 `detailRecord` 上下文，保证详情头部、任务列表、时间轴和审批区始终指向同一条记录。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 272：周报管理页完成“审批流程动态化展示与前端逻辑去写死”最小闭环，当前 `WeeklyWorkEditView.vue` 已新增统一的 `flowNodes` 兼容层，优先从记录上的流程节点数组字段取值，若后端暂未返回完整节点数组，则使用现有 `currentApprovalNode / status / availableReturnTargets` 兼容映射成可扩展的节点结构；步骤条、当前审批节点文案、记录卡节点摘要、详情弹层节点摘要和审批操作区提示现都基于这套动态节点结构渲染，不再在模板层写死“四段固定岗位”。退回目标展示也已从原先的固定四级规则切换成“当前节点之前的节点列表动态生成”，若后端已返回 `availableReturnTargets` 则前端会优先按该集合与流程节点交集展示，既兼容旧模型，又为后续按组织架构动态生成超管之下的审批流预留了前端结构。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 273：周报管理页完成“审批节点名称按组织架构真实岗位显示”最小闭环，当前 `WeeklyWorkEditView.vue` 已把 `flowNodes` 的节点文案统一收口到“真实岗位名称优先”的前端兼容层：节点展示会优先读取 `postName / positionName / orgRoleName / jobTitle` 等真实岗位字段，其次再取非泛化的展示名，只有在前端与现有接口都拿不到更真实的岗位信息时才兜底到旧的泛化角色名。当前周报区步骤条、当前审批节点摘要、记录卡、详情弹层头部、审批操作区与退回目标选项现都复用同一套 `approvalNodeLabel / buildFlowNodeLabel` 逻辑，避免一处显示真实岗位、一处退化回“科长（主任）/分管领导”之类旧文案；同时对当前填报人节点补充了“用户姓名 + 岗位”组合兜底，尽量让首节点也更接近真实组织岗位语义。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-20 weeklywork 闭环 274：周报管理页完成“前端审批链按组织架构树优先推导”最小闭环，当前 `WeeklyWorkEditView.vue` 已引入基于现有 `/orgtree/ancestors/{userId}` 的组织链缓存与候选收集层：会先按提报人 `userId` 拉取并缓存上级链，再优先用“提报人节点 + 上级链”推导 `flowNodes`，只有当组织链与记录显式节点数组都不足时，才回落到旧四级兼容模型。新的 `buildFlowNodes()` 现已调整为“组织树优先 -> 记录显式 flowNodes -> 旧模型兜底”，并为组织链节点补齐 `aliases/source` 元数据，使当前审批节点高亮、节点名称摘要、退回目标生成都能基于同一条动态链路完成匹配；退回目标仍优先提交后端可识别的旧编码，但前端排序与展示已改为按当前节点之前的实际组织链节点生成，避免继续以前端固定四段为主逻辑。验证结果：`npm run build` 通过；Vite chunk size warning 按要求未处理。
- 2026-03-27 statistics 闭环 489：按“最小闭环、避免回归”先收紧工作评分可见范围，再开始补统计分析页可用性。当前前端已将 `ScoreView` 路由、侧边菜单和首页普通用户快捷入口统一收口为仅管理员可见，超管以下用户不再看到“工作评分”入口；同时 `StatisticsOverviewView.vue` 新增“当前查询工作面”摘要卡，统一展示当前周次、组织范围与趋势实际口径，并补一键“清空当前查询条件”入口，继续保持现有统计接口协议、查询条件和刷新链路不变。验证结果：`npm run build` 通过。
- 2026-03-27 statistics 闭环 490：继续按“只做前端可用性增强、不改接口协议”收口统计分析页摘要卡体验。`StatisticsOverviewView.vue` 现已在“当前查询工作面”摘要卡中补齐统一的视图模式提示：存在筛选条件时明确显示“当前为筛选结果视图”，并补充“当前数据基于已填写查询条件生成”的说明；无筛选条件时则明确显示“当前为默认全量视图”，同步说明当前未附加额外筛选条件。摘要卡同时继续稳定展示当前周次、当前组织范围、当前趋势展示范围与筛选状态标签，首次进入页面也不会留空；“清空当前查询条件”按钮在无有效筛选时会继续禁用并弱化，避免误导。现有查询、刷新、概览、排名、红黄牌和趋势接口调用方式保持不变。验证结果：`npm run build` 通过。
- 2026-03-27 statistics 闭环 491：继续按“只做前端最小增强、不改接口协议”收口统计分析页三态反馈。`StatisticsOverviewView.vue` 已为总体概览、组织排名、红牌名单、黄牌名单和最近4周趋势分别补齐“加载中 / 无数据 / 有数据”状态展示：加载时会显示明确的轻量提示块，不再让区块大面积空白；无数据时则按当前视图模式区分文案，筛选结果视图下统一提示“当前筛选条件下暂无数据，可调整条件后重试”，默认全量视图下则提示“当前暂无统计数据”或“当前暂无可展示记录”。现有查询、刷新、概览、排名、红黄牌和趋势接口调用方式保持不变。验证结果：`npm run build` 通过。
- 2026-03-27 statistics 闭环 492：继续按“只做前端最小展示、不改接口协议”收口统计分析页查询结果联动提示。`StatisticsOverviewView.vue` 已新增页面内“本次查询结果说明”提示区，并以 `lastActionType` 记录最近一次动作来源：首次进入默认展示“当前展示默认全量统计结果”，查询后会明确提示“已按当前筛选条件加载统计结果”，清空条件后会提示“已恢复默认全量统计视图”，刷新后会提示“已重新加载当前条件下的统计结果”；加载中时，提示区也会同步切到“正在按当前条件加载统计数据”。提示文案同时复用当前周次、组织范围和默认全量/筛选结果视图状态，帮助用户确认概览、排名、红黄牌和趋势区块都已按当前条件同步更新。现有查询、刷新、概览、排名、红黄牌和趋势接口调用方式保持不变。验证结果：`npm run build` 通过。

- 2026-03-29 AI stability 闭环 493：按“先恢复运行，再继续功能”原则，先做月报链路启动验证。真实启动时定位到 `backend/src/main/resources/mapper/agent/AgentSessionMapper.xml` 中 `cited_chunk_ids <> ''` 属于非法 XML 字符，导致 MyBatis 解析失败、`sqlSessionFactory` 创建失败，进而让 `ApplicationContext` 启动中断。当前已将该条件修正为 `cited_chunk_ids &lt;&gt; ''`，并完成最小回归验证：`mvn -q -DskipTests compile` 通过、`npm.cmd run build` 通过；使用 `mvn -q -DskipTests -Dspring-boot.run.profiles=wsl -Dspring-boot.run.jvmArguments=-DDB_HOST=127.0.0.1 -Dspring-boot.run.arguments=--server.port=18080 spring-boot:run` 实际启动成功，日志已确认 `Started LectureTeamApplication`。默认 8080 端口当前被本机其他进程占用，这一项属于环境占用而非代码故障。
- 2026-03-29 AI ux 闭环 494：按“统一入口、最小侵入、不改业务逻辑”的原则，为现有功能页补齐操作说明体系。当前已将问号说明入口统一收口到 rontend/src/components/layout/AppPageShell.vue，按路由自动映射到帮助配置；已重写 rontend/src/components/PageHelp.vue，统一触发按钮、弹层文案与首次进入提示样式；已补齐 rontend/src/config/pageHelp.js，覆盖首页、单位管理、参数配置、操作日志、组织架构、用户管理、周报管理、考勤管理、统计分析、工作评分、个人中心、知识库管理、AI接入区、AI权限配置、Skills中心、AI工作台、咨询台账、月度报表、专家台账等现有功能页的中文操作说明。验证结果：mvn -q -DskipTests compile 通过，
pm.cmd run build 通过；前端仍保留既有 Vite chunk size warning，本轮未处理。- 2026-03-29 AI ux 闭环 495：按“只补页面引导、不改业务逻辑”的原则，继续补齐后续新增页面的引导能力。当前已在 `frontend/src/config/pageHelp.js` 中重写并统一整理所有现有页面的操作说明与引导步骤，覆盖首页、单位管理、参数配置、操作日志、组织架构、用户管理、周报管理、考勤管理、统计分析、工作评分、个人中心、知识库管理、AI 接入区、AI 权限配置、Skills 中心、AI 工作台、咨询台账、月度报表、专家台账；同时复用既有页面结构选择器并接入本轮已补好的 `data-guide` 锚点，使后续新增页面也具备类似组织架构页的分步引导体验。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 496：按“只收口页面引导体验、不扩业务功能”的原则，继续优化帮助体系的首访提示频率。当前已重写 `frontend/src/components/PageHelp.vue` 与 `frontend/src/components/PageGuide.vue`，统一修复帮助弹层与引导浮层的中文文案显示，并把自动首访提示调整为“同一登录会话内最多自动弹出一次”；页面级首次访问记录仍按 `pageKey + 用户` 写入本地存储保留，后续仍可通过标题旁问号随时查看说明或重新开始引导。这样可以避免用户在连续打开多个页面时反复被首访提示打断，同时不影响既有问号入口和分步引导能力。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 497：按“只优化页面引导高亮与滚动体验、不扩业务功能”的原则，继续收口引导组件交互。当前已重写 `frontend/src/components/PageGuide.vue`，把引导逻辑调整为“切换步骤时只在目标区域明显超出视口时滚动一次；页面滚动或窗口尺寸变化时仅刷新高亮位置，不再反复触发 `scrollIntoView` 抢焦点”，并为高亮位置刷新补上 `requestAnimationFrame` 节流，避免引导浮层在连续滚动时出现抖动、抢滚动或高亮框跳动。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 498：按“只补引导目标缺失时的兜底提示、不扩业务功能”的原则，继续收口页面引导稳定性。当前已重写 `frontend/src/components/PageGuide.vue`，当页面引导步骤对应的目标元素全部缺失或页面结构临时未完成渲染时，不再直接关闭引导，而是显示居中的兜底提示卡片，明确说明“页面可能尚未加载完成或结构已变化”，并提供“重新检查”和“关闭”两个明确动作；已有高亮、滚动、步骤切换逻辑保持不变。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 499：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口咨询台账页的真实操作路径。当前已在 `frontend/src/config/pageHelp.js` 中将 `aiLedger` 的引导顺序从“先看统计”调整为“先筛选 -> 再看统计 -> 最后看列表”，并同步把帮助文案、首次进入提示和列表步骤说明改为更贴近实际使用路径的表达，避免用户首次进入咨询台账时先看见结果区却不知道应先收口筛选条件。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 500：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口月度报表页的真实操作路径。当前已在 `frontend/src/config/pageHelp.js` 中将 `aiMonthlyReport` 的帮助文案收口为“先筛选、再看概览和趋势、最后看排行并导出”，并把引导步骤调整为“报表筛选区 -> 年度概览区 -> 趋势对比区 -> 排行与专家区”，使月度报表首次引导更符合真实阅读顺序，也避免用户一进入页面就直接关注排行或导出而忽略年份和统计口径。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 501：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口 AI 工作台页的真实操作路径。当前已在 `frontend/src/views/agent/AIWorkbenchView.vue` 中为“会话配置”区补充独立 `data-guide` 锚点，并在 `frontend/src/config/pageHelp.js` 中将 `aiWorkbench` 的帮助文案和首次提示收口为“先确认权限与专家身份 -> 再配置会话 -> 再提问 -> 最后回看引用与历史”，引导步骤也已调整为“能力确认区 -> 会话配置区 -> 提问区 -> 会话记录区 -> 历史会话区”，使工作台引导顺序与页面真实结构一致。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 502：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口 AI 权限配置页的真实操作路径。当前已在 `frontend/src/config/pageHelp.js` 中将 `aiPermissions` 的帮助文案、首次进入提示和步骤说明统一收口为“先选用户 -> 再按 AI/知识库/技能三层逐块保存 -> 最后在结果区回看并点选回填继续调整”，进一步强调了权限配置页最真实的操作顺序，也减少了用户首次进入时把三块表单和下方回显结果看成彼此割裂区域的理解成本。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 503：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口 Skills 中心页的真实操作路径。当前已在 `frontend/src/views/skill/SkillListView.vue` 中为“知识库绑定 / 验证题维护”区补充独立 `data-guide="skills-binding"` 锚点，并在 `frontend/src/config/pageHelp.js` 中将 `skills` 的帮助文案和首次提示统一收口为“先选技能 -> 再看版本与配置 -> 再做知识库绑定和验证题准备 -> 最后执行验证”，引导步骤也已同步调整为“技能列表区 -> 版本配置区 -> 绑定与验证题区 -> 验证区”，使 Skills 页首次引导和页面真实结构保持一致。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 504：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口知识库管理页的真实操作路径。当前已在 `frontend/src/views/knowledge/KnowledgeBaseListView.vue` 中将 `knowledge-import` 引导锚点前移到整个“文档导入”区块，不再只高亮文件选择框；同时在 `frontend/src/config/pageHelp.js` 中将 `knowledge` 的帮助文案和首次提示统一收口为“先看并切换知识库 -> 再导入文档 -> 最后检索核对”，引导步骤也已同步调整为“知识库区 -> 文档导入区 -> 知识检索区”，使知识库页的首次引导更符合真实工作顺序。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-29 AI ux 闭环 505：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口 AI 接入区页的真实操作路径。当前已在 `frontend/src/views/ai/AIProviderConfigView.vue` 中为“最近一次测试结果”区补充独立 `data-guide="ai-provider-test-result"` 锚点，并在 `frontend/src/config/pageHelp.js` 中将 `aiProvider` 的帮助文案和首次提示统一收口为“先配接入 -> 再测试连通 -> 最后回看测试结果和接入列表”；引导步骤也已同步调整为“接入配置区 -> 接入列表区 -> 测试结果区”。本轮过程中曾因 `aiProvider.helpText` 缺失结尾文本导致 `pageHelp.js` 语法错误、前端构建失败；已立即补齐该段 `使用提示` 后重新验证，最终 `mvn -q -DskipTests compile` 与 `npm.cmd run build` 均恢复通过。未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-30 AI ux 闭环 506：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口专家台账页的真实操作路径。当前已在 `frontend/src/config/pageHelp.js` 中将 `experts` 的帮助文案和首次提示统一收口为“先做专家绑定 -> 再回看台账结果 -> 必要时点选回填继续调整”，进一步强调了专家身份通常建立在技能与权限已完成配置的前提下，也让“绑定区”和“台账区”之间的关系更清晰。引导步骤仍保持为“专家绑定区 -> 专家列表区”，但步骤说明已同步改成更贴近实际操作顺序的表达。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-30 AI ux 闭环 507：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口个人中心页的 AI 说明顺序。当前已在 `frontend/src/views/profile/ProfileView.vue` 中为“AI 授权概览 / 专家身份 / 当前状态”补充独立 `data-guide` 锚点，并在 `frontend/src/config/pageHelp.js` 中将 `profile` 的帮助文案和首次提示统一收口为“先确认账号与 AI 授权 -> 再看专家身份 -> 最后确认 AI 工作台状态”，避免原先使用通用 `.profile-grid` 选择器时命中到非 AI 重点区块。引导步骤也已同步改为“AI 授权概览区 -> 专家身份区 -> 当前状态区”。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-30 AI ux 闭环 508：按“只微调关键页面引导顺序与提示语、不扩业务功能”的原则，继续收口首页的 AI 说明顺序。当前已在 `frontend/src/config/pageHelp.js` 中将 `home` 的帮助文案和首次提示统一收口为“先确认身份与菜单范围 -> 再看快捷入口是否已有 AI 页面 -> 最后在可访问模块区确认自己是否已开通对应能力”，进一步把首页从“泛入口页”收口成“进入 AI 模块前的第一道权限与入口确认页”。引导步骤仍保持为“账号概览区 -> 快捷入口区 -> 可访问模块区”，但步骤说明已同步改成更贴近 AI 使用路径的表达。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-30 AI ux 闭环 509：按“只做帮助文案一致性收口、不扩业务功能”的原则，对 AI 相关页面的缺权排查提示做了一次轻量统一。当前已在 `frontend/src/config/pageHelp.js` 中同步收口首页、个人中心、AI 权限配置页和 AI 工作台页的“缺少入口 / 没有可选技能或知识库 / 不确定是否已授权”提示语，统一引导用户优先按“首页确认入口 -> 个人中心核对授权摘要 -> 管理员在 AI 权限配置页核查”这条路径排查，减少用户在多个 AI 页面间来回猜问题来源。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
- 2026-03-30 AI ux 闭环 510：按“只做帮助弹层统一提示能力、不扩业务功能”的原则，继续收口 AI 相关页面的帮助体验。当前已在 `frontend/src/components/PageHelp.vue` 中新增可选的“排查提示”展示区，并在 `frontend/src/config/pageHelp.js` 中为首页、个人中心、AI 权限配置页和 AI 工作台页补充统一的 `troubleshootingHint`，让用户点击问号后可以直接看到一致的缺权排查路径，而不必在多页说明文案中自行拼接判断。验证结果：`mvn -q -DskipTests compile` 通过，`npm.cmd run build` 通过；未修改后端业务、接口、数据库结构，未发现已知回归。
