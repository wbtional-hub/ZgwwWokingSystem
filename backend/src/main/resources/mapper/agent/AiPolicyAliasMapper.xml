<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lecturesystem.modules.agent.mapper.AiPolicyAliasMapper">
    <resultMap id="AiPolicyAliasMap" type="com.example.lecturesystem.modules.agent.entity.AiPolicyAliasEntity">
        <id property="id" column="id"/>
        <result property="baseId" column="base_id"/>
        <result property="regionScope" column="region_scope"/>
        <result property="policyKey" column="policy_key"/>
        <result property="alias" column="alias"/>
        <result property="priority" column="priority"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="batchInsert">
        INSERT INTO ai_policy_alias (
            base_id, region_scope, policy_key, alias, priority, enabled, created_at, updated_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.baseId}, #{item.regionScope}, #{item.policyKey}, #{item.alias}, #{item.priority}, #{item.enabled}, #{item.createdAt}, #{item.updatedAt}
            )
        </foreach>
    </insert>

    <insert id="insert" parameterType="com.example.lecturesystem.modules.agent.entity.AiPolicyAliasEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO ai_policy_alias (
            base_id, region_scope, policy_key, alias, priority, enabled, created_at, updated_at
        ) VALUES (
            #{baseId}, #{regionScope}, #{policyKey}, #{alias}, #{priority}, #{enabled}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <delete id="deleteByBaseId">
        DELETE FROM ai_policy_alias WHERE base_id = #{baseId}
    </delete>

    <select id="countByBaseId" resultType="int">
        SELECT COUNT(1)
        FROM ai_policy_alias
        WHERE base_id = #{baseId}
    </select>

    <select id="queryEnabledByBaseId" resultMap="AiPolicyAliasMap">
        SELECT id, base_id, region_scope, policy_key, alias, priority, enabled, created_at, updated_at
        FROM ai_policy_alias
        WHERE base_id = #{baseId}
          AND enabled = TRUE
        ORDER BY priority DESC, id ASC
    </select>
</mapper>
