<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lecturesystem.modules.agent.mapper.AiPolicyEvalCaseMapper">
    <resultMap id="AiPolicyEvalCaseMap" type="com.example.lecturesystem.modules.agent.entity.AiPolicyEvalCaseEntity">
        <id property="id" column="id"/>
        <result property="baseId" column="base_id"/>
        <result property="regionScope" column="region_scope"/>
        <result property="question" column="question"/>
        <result property="expectedPolicyKey" column="expected_policy_key"/>
        <result property="expectedQuestionType" column="expected_question_type"/>
        <result property="expectedAnswerContains" column="expected_answer_contains"/>
        <result property="expectedForbiddenKeywords" column="expected_forbidden_keywords"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="batchInsert">
        INSERT INTO ai_policy_eval_case (
            base_id, region_scope, question, expected_policy_key, expected_question_type,
            expected_answer_contains, expected_forbidden_keywords, enabled, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.baseId}, #{item.regionScope}, #{item.question}, #{item.expectedPolicyKey}, #{item.expectedQuestionType},
                #{item.expectedAnswerContains}, #{item.expectedForbiddenKeywords}, #{item.enabled}, #{item.createdAt}
            )
        </foreach>
    </insert>

    <insert id="insert" parameterType="com.example.lecturesystem.modules.agent.entity.AiPolicyEvalCaseEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO ai_policy_eval_case (
            base_id, region_scope, question, expected_policy_key, expected_question_type,
            expected_answer_contains, expected_forbidden_keywords, enabled, created_at
        ) VALUES (
            #{baseId}, #{regionScope}, #{question}, #{expectedPolicyKey}, #{expectedQuestionType},
            #{expectedAnswerContains}, #{expectedForbiddenKeywords}, #{enabled}, #{createdAt}
        )
    </insert>

    <delete id="deleteByBaseId">
        DELETE FROM ai_policy_eval_case WHERE base_id = #{baseId}
    </delete>

    <select id="countByBaseId" resultType="int">
        SELECT COUNT(1)
        FROM ai_policy_eval_case
        WHERE base_id = #{baseId}
    </select>

    <select id="queryEnabledByBaseId" resultMap="AiPolicyEvalCaseMap">
        SELECT id, base_id, region_scope, question, expected_policy_key, expected_question_type,
               expected_answer_contains, expected_forbidden_keywords, enabled, created_at
        FROM ai_policy_eval_case
        WHERE base_id = #{baseId}
          AND enabled = TRUE
        ORDER BY id ASC
    </select>
</mapper>
