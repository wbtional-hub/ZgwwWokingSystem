<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lecturesystem.modules.agent.mapper.AiPolicyFeedbackMapper">
    <resultMap id="AiPolicyFeedbackMap" type="com.example.lecturesystem.modules.agent.entity.AiPolicyFeedbackEntity">
        <id property="id" column="id"/>
        <result property="baseId" column="base_id"/>
        <result property="userId" column="user_id"/>
        <result property="sessionId" column="session_id"/>
        <result property="rawQuestion" column="raw_question"/>
        <result property="normalizedQuestion" column="normalized_question"/>
        <result property="intentId" column="intent_id"/>
        <result property="feedbackType" column="feedback_type"/>
        <result property="feedbackText" column="feedback_text"/>
        <result property="finalAnswer" column="final_answer"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.lecturesystem.modules.agent.entity.AiPolicyFeedbackEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO ai_policy_feedback (
            base_id, user_id, session_id, raw_question, normalized_question, intent_id,
            feedback_type, feedback_text, final_answer, created_at
        ) VALUES (
            #{baseId}, #{userId}, #{sessionId}, #{rawQuestion}, #{normalizedQuestion}, #{intentId},
            #{feedbackType}, #{feedbackText}, #{finalAnswer}, #{createdAt}
        )
    </insert>

    <delete id="deleteByBaseId">
        DELETE FROM ai_policy_feedback WHERE base_id = #{baseId}
    </delete>

    <select id="countByIntentAndType" resultType="int">
        SELECT COUNT(1)
        FROM ai_policy_feedback
        WHERE base_id = #{baseId}
          AND intent_id = #{intentId}
          AND feedback_type = #{feedbackType}
    </select>

    <select id="countByReviewSignal" resultType="int">
        SELECT COUNT(1)
        FROM ai_policy_feedback
        WHERE base_id = #{baseId}
          AND feedback_type = #{feedbackType}
    </select>

    <select id="queryRecentByBaseId" resultMap="AiPolicyFeedbackMap">
        SELECT *
        FROM ai_policy_feedback
        WHERE base_id = #{baseId}
        ORDER BY created_at DESC, id DESC
        LIMIT 100
    </select>
</mapper>
