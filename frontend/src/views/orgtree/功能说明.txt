前端模块名称：orgtree

页面职责：
组织架构模块。负责无限级树结构、创建下级、节点移动、递归查询、tree_path 管理。是整个系统最核心模块之一。与 user、permission、statistics 强关联。

与其他页面关系：
1. 所有页面统一由 router 管理。
2. 所有接口统一从 src/api 发起。
3. 页面只处理展示与交互，不做权限真校验。
4. 权限是否可用以后端返回为准。
